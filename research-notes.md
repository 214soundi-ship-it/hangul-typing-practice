# 연구 노트 — 타이핑 기반 사전학습 프로그램

> 소논문 집필을 위한 작업 기록. 결정마다 "왜 그렇게 했는지" 이유를 남겨서, 나중에 논문 Method/설계 근거로 그대로 가져다 쓸 수 있게 정리함.

## 1. 연구 배경과 목적

실제 강의를 담당하는 연구자(글쓴이 본인)가 외국인 유학생 대상 한국어 글쓰기 수업을 운영하면서 관찰한 문제 두 가지에서 출발.

**관찰된 문제**
1. 일부 학습자는 글의 내용을 못 떠올려서가 아니라, 한글 자판에 익숙하지 않아 생각을 입력하는 데 시간이 오래 걸림. 자판에서 글자 찾기·오타 수정·띄어쓰기/문장부호 처리 과정에서 글쓰기 흐름이 끊김.
2. 다음 차시 교재 지문을 미리 읽어오는 건 학습자 자율에 맡겨져 있어 준비 정도 차이가 큼. 지문을 충분히 이해하지 못하면 그 지문으로 설명되는 글쓰기 수업 내용(개념, 작성법)도 따라가기 어려움.

**핵심 아이디어**
> "한글 타이핑을 연습해야 한다면, 다음 글쓰기 수업에서 실제로 배우고 이해해야 할 내용을 타이핑하게 하자."

의미 없는 연습 문장 대신 다음 수업의 핵심 개념어·지문 어휘·표현·문장을 타이핑 자료로 써서, **지문 사전 이해**와 **타이핑 연습**을 하나의 활동으로 묶음.

이 프로그램 자체가 한국어 쓰기교육 분야 소논문의 **연구 도구**이기도 함 — 만드는 이유이자 데이터를 모으는 수단.

## 2. 연구 목표와 연구문제 (2026-08-26 확정)

**확정 연구 주제**: "학문 목적 한국어 학습자를 위한 내용 기반 타이핑 활동의 효과: 수업 내용 이해도와 쓰기 효능감을 중심으로"

**연구문제** (2026-08-27, 측정 방법 세부 문구 보강)
- RQ1 (사전 내용 이해도): 교재 지문 타이핑 활동이 유학생들의 글쓰기 수업 내용(핵심 어휘 및 텍스트) 이해에 미치는 실질적인 영향은 어떠한가?
  - 측정 방법: 타이핑 직후 진행하는 내용 확인 퀴즈 점수. `quiz_results` 테이블(2~3주차 프로그램에 구현 완료, §5 항목 49) + 어휘 재인 문항과 별도로 지문 내용 이해 문항(항목 56)으로 직접 수집
- RQ2 (글쓰기에 대한 인식 변화 · 쓰기 효능감): 타이핑 활동을 통한 수업 내용 숙지가 학습자의 실제 글쓰기 과제 수행 시 느끼는 심리적 부담감(쓰기 불안) 감소 및 효능감에 긍정적인 영향을 미치는가?
  - 측정 방법: 학기 말 리커트 척도 문항을 활용한 설문조사 (문항 설계 아직 미착수 — §6). 이 프로그램이 "도움이 됐다고 느꼈는지"를 묻는 인식 문항도 포함 예정(§5 항목 55 논의 — 퀴즈의 객관적 이해도 측정과는 별개로, 주관적 인식을 보완적으로 수집)
- RQ3 (전반적 만족도): 학습자들은 타이핑 활동의 효용성에 대해 어떻게 인식하고 있는가?
  - 측정 방법: 학기 말 설문조사의 서술형 문항이나 가벼운 면담을 통해 질적 데이터로 확보 (설계 아직 미착수 — §6)

**이론적 배경** (2026-08-27, 세부 논리 보강)
1. **학문 목적 한국어 수업에서의 사전학습과 내용 이해** — 대학 교양 글쓰기 수업에서 유학생은 어휘/지문 이해와 글쓰기를 동시에 수행해야 하는 "인지적 이중고"를 겪는다. 성공적인 학술적 글쓰기를 위해서는 수업 전 교재 지문·핵심 어휘에 대한 사전학습이 필수적이지만, 기존의 자율적 읽기 과제("미리 읽어오기")는 학습자 참여도에 크게 의존해 이를 담보하지 못한다 — 이를 확실히 담보할 사전학습 기제가 필요하다는 문제의식
2. **플립드 러닝의 사전학습 원리를 적용한 타이핑 활동** — 전면적인 플립드 러닝 수업 모형이 아니라, 그 핵심인 "지식 입력(input)의 교실 밖 배치" 원리만 차용했음을 명확히 함(과도한 이론적 주장 회피). 교재 내용을 단순히 눈으로 읽는 게 아니라 직접 "타이핑"하게 하는 것은 학습자를 목표 텍스트에 강제로 노출시키고 주의를 집중(Noticing)하게 만드는 적극적·능동적 사전학습 활동이라는 논리
3. **디지털 한국어 쓰기 환경에서 한글 입력 능력과 학습 부담** — 타자 속도 향상 자체가 목적이 아니라, 디지털 환경에서의 과제 수행 능력이 유학생의 심리적 부담감(쓰기 불안)과 직결된다는 데 초점. 한글 타자의 미숙함은 단순 기능 부족이 아니라 실제 글쓰기 내용 구성에 쓰여야 할 인지적 에너지를 빼앗는 요인이며, 사전 타이핑 활동이 이 물리적 장벽을 덜어주어 본 수업에서 학습자가 "글을 계획하고 작성하는 데" 온전히 몰입하도록 돕는다는 논리 — 타이핑 유창성 자체를 별도 RQ로 측정하지는 않되, RQ2(쓰기 효능감)에 영향을 주는 배경 기제로 이론적 틀에 포함
   - 세 축을 합치면 이 연구는 "타자 연습기 개발"이 아니라 "유학생이 본 강의를 잘 따라올 수 있도록 준비시키는 교육학적 스캐폴딩(scaffolding) 연구"로 포지셔닝됨

**(이전 버전과의 관계)** 기존에 있던 "RQ2: 타이핑 유창성 자체의 변화(속도·정확도, 글쓰기 착수 지연)"는 위 확정안에서 독립된 연구문제로는 빠지고 "타이핑 지연을 줄이는 것이 글쓰기 효능감 부담을 던다"는 배경 논리(위 3번)로 흡수됨. §4에 정리된 설계 결정(정확도·속도 측정 등)은 그대로 유효하며, RQ2의 직접 근거 자료는 아니어도 도구의 부수 효과를 보여주는 참고 데이터로 계속 수집·보관함.

**연구윤리**: 연구 참여와 수업 성적 평가는 분리, 동의·철회·익명성 보장 필요. 소논문 투고 학술지가 아직 미확정이라 IRB(생명윤리위원회) 심의 필요 여부를 별도 확인 중(§6).

## 3. 전체 프로그램 구조

| 단계 | 내용 | 강의 지문 연계 | 파일 | 상태 |
|---|---|---|---|---|
| 1주차 | 자판 자리 연습 (자모 위치·손가락 사용법, 10단계: 기본자리→왼손윗→왼손아래→가운데자리→오른손윗→오른손아래→전체자리→받침과 띄어쓰기→이중모음→겹받침) + 단어비 게임 | 무관 (자판 자체가 처음인 학습자를 위한 순수 기초 단계) | `stage0_jari_practice.html` | ✅ 완성 |
| 2~3주차 | 성찰적 글 — 파트 1(계획하기)/파트 2(작성하기)/파트 3(수정하기), 각 파트는 실제 수업 진행과 동일 | 실제 강의 주2~3 (성찰, 감정표현·비유 / 교재 p23~44) | `week2_3_reflective_writing.html` | ✅ 파트 1·2·3(23~44쪽) 모두 완성·적용됨 |
| 4~5주차 | 요약하기 — 파트 1(45~54쪽)/파트 2(55~61쪽, 혐오 표현 예제)/파트 3(62~64쪽, AI 지문 — 문어체 코너는 프로그램에서 제외) | 실제 강의 주4~5 (요약, 의견 및 근거 제시 / 교재 p45~64) | `week4_5_summary.html` | 🟡 파트 1~3 전부 어휘·지문·이해도 문항 SQL·앱 반영 완료, 사용자의 최종 SQL 실행만 남음 |

**최종 범위 확정(2026-08-27)**: 프로그램은 **1주차(자판 자리 연습) / 2~3주차(성찰적 글) / 4~5주차(요약하기)** 3개 큰 파트로 확정하고, 원래 계획에 있던 **6~15주차(보고서 쓰기)는 만들지 않기로 결정**함. 사유: (1) 보고서 쓰기는 유형→주제→참고문헌→개요→서론→인용→조사→결론→수정까지 하위 단계가 많아 제작 분량이 지나치게 큼 (2) 연구 목적상 이미 만든 2개 단위(1주차·2~3주차)만으로도 도구 효과를 보여주기에 충분하고, 세 번째 단위를 더 만들어도 방법론적으로 반복 검증일 뿐 새로운 정보를 크게 얻지 못한다고 판단(사용자 표현: "두 개에서만 해봐도 동일한 것, 결국 반복"). `index.html`에서 "6~15주차 보고서 쓰기" 카드 제거.

1주차와 2~3주차 이후는 서로 독립된 별개 프로그램(자동 연계 없음, §4 참고). `week1_hangul_typing.html`(일반 문장 9문항)은 1주차가 0단계를 흡수하며 랜딩 페이지에서 제외됨 — 파일은 남아있으나 미사용.

실제 대응 강의: 2026-2학기 고려대 글쓰기(외국인 중급), 강의계획서 기준(`강의 자료 및 정보/2026-2 글쓰기 강의 소개.pdf`, 로컬 보관·비공개). 2주차 이후 콘텐츠(핵심 개념어·지문 어휘·핵심 문장)는 교재 원문이 필요한 시점에 해당 쪽수만 받아서 제작 — 교재 PDF 원본은 저작권 문제로 공개 저장소에 올리지 않고 로컬에만 보관(`.gitignore` 제외). 교재에서 발췌한 지문 자체(개념어·어휘·문장 원문)도 같은 이유로 Supabase DB에만 저장하고 공개 소스코드(HTML)에는 하드코딩하지 않음 — §4 참고.

## 4. 설계 결정과 근거 (논문 Method 초안용)

| 결정 | 근거 |
|---|---|
| 0단계(자리연습)를 별도로 둠 | 학습자 다수가 한글 타이핑 자체가 처음인 외국인. 자판 위치를 모르는 상태에서 곧바로 문장 타이핑을 시키면 "타이핑 유창성" 측정값이 자판 숙지 여부 자체에 의해 왜곡됨 |
| "0단계"를 별도 사전 단계가 아니라 "1주차"로 재편입하고, 기존에 따로 있던 1주차 문장 타이핑(week1_hangul_typing.html, 강의 지문과 무관한 일반 문장 9문항)은 랜딩 페이지에서 제외 | 0단계가 8~10단계(받침·이중모음·겹받침)까지 확장되며 그 자체로 1주차 분량을 채울 만큼 커졌고, 매 문항 끝 스페이스바 확정 동작이 띄어쓰기 습관 형성에도 어느 정도 기여함. 결정적으로는, 이 도구의 핵심 설계 철학이 "의미 없는 연습 문장 대신 실제 강의 지문으로 타이핑 연습을 겸한다"인데 week1의 일반 문장(예: "오늘은 날씨가 맑고...")은 그 철학과 반대됨 — 문장 단위 타이핑은 2~3주차(성찰적 글)에서 실제 지문으로 다루는 게 더 의미 있다고 판단. 다만 week1이 유일하게 다루던 "문장부호의 실전 문맥 사용"은 2~3주차 설계 시 빠뜨리지 않아야 함(미정 사항에 기록) |
| 손가락 안내를 텍스트 대신 손 모양 다이어그램으로 표시 | 학습자가 한국어 어휘를 몰라도(예: "왼손 약지"라는 말을 몰라도) 이해 가능하도록 |
| 정확도 계산식을 화면마다 동일하게 유지 ("현재까지 입력한 글자 중 정답과 일치하는 비율") | 연구 데이터 수집이 목적이므로 측정값 정의의 일관성이 결과 신뢰도에 직결됨 |
| 디자인 톤을 종이/teal 계열의 차분한 색상으로 유지 (화려한 색 배제) | 연구도구로서 방법론적 중립성 확보. 자극적인 색이 타이핑 속도 측정에 각성 효과 등 혼란변수로 작용할 가능성 배제. 대신 버튼 크기·대비·선택 상태 강조 등 구조적 요소로 학습자(외국인 초급 학습자) 대상 명확성 확보 |
| 0단계를 7단계로 재구성, "가운데자리"(ㅅㅛㅎㅗㅠㅜ 6개, 양손 검지 이동키)를 정규 단계로 승격 | 검지가 홈로우에서 위·아래로 뻗는 자리가 특히 헷갈리기 쉬워 손 단위(왼손 다→가운데→오른손 다)로 단계를 재배열. 기본자리는 ㅁㄴㅇㄹㅓㅏㅣ+;(오른쪽 새끼손가락 홈 포지션)로 축소 |
| 자판 다이어그램에서 "기본자리 자모만" 항상 밝게, 나머지는 항상 흐리게 | 기존엔 물리적 홈row 전체(ㅁㄴㅇㄹㅎㅗㅓㅏㅣ)를 항상 밝게 표시해서, 가운데자리로 옮겨간 ㅎㅗ가 여전히 "기본자리인 것처럼" 보이는 문제가 있었음. 실제 소속 단계 기준으로 표시하도록 수정 |
| 같은 글자 연속 3회가 아니라, 1회 순서대로 도입 후 나머지는 무작위로 흩어서 총 3회 연습 | 연속 반복은 단순 암기라 실전 타이핑과 괴리. 첫 바퀴는 정해진 순서로 1회씩 접하게 하고(도입), 이후 무작위로 섞어 총 REPS_PER_KEY(3)회 만나게 해서 실제 회상 능력을 기름. 인접 중복은 후처리로 제거 |
| 정답 시 자동 진행으로 잠시 바꿨다가(스페이스바 확인 방식 폐지), 다시 스페이스바 확인 방식으로 되돌림. 오답 시에는 소리(비프음)로 피드백 | 처음엔 "매 문항마다 추가 입력이 필요해 흐름이 끊긴다"는 이유로 자동 진행으로 바꿨었음. 그런데 스페이스바는 보통 엄지손가락으로 누르므로, 매 문항 끝에 스페이스바를 누르게 하면 그 동작 자체가 손을 홈 포지션(자판 기본 자리)으로 되돌려놓는 역할을 함 — 초급 학습자가 자판 위치를 몸으로 익히는 이 도구의 목적에는 자동 진행보다 스페이스바 확인 쪽이 더 맞다고 판단해 재도입. 오답 시엔 소리(경고음)로 즉시 알려줌 |
| Shift가 필요한 항목(쌍자음·이중모음) 연습 중에는 실제로 눌러야 할 Shift 키(반대쪽 손)도 자판 다이어그램에서 함께 강조 | 기존엔 base 키만 강조되어, 초급 학습자가 Shift를 눌러야 한다는 안내 문구는 봐도 "어느 Shift"인지 자판 그림에서 바로 확인할 수 없었음. 실제 타이핑 관행(반대손 Shift 사용)에 맞춰 강조 위치를 계산 |
| 기록 화면에 같은 단계를 몇 번째 연습했는지(회차)와, 직전 회차 대비 속도 변화(▲▼)를 표시 | "반복 연습하면 실제로 빨라지는가"를 연구 결과로 보여주려면 단순 나열이 아니라 회차별 비교가 필요. 단계마다 글자 수가 달라도 공정하게 비교하도록 시간이 아니라 속도(글자 수÷시간)를 기준으로 비교 |
| 무입력 20초(또는 탭 전환) 감지 시 자동으로 화면을 멈추고, "이어서 하기"를 눌러야 재개되게 함. 쉰 횟수·시간을 별도로 기록해 속도 계산에서 제외 | 학생이 연습 중 자리를 비우거나 딴짓을 하면 벽시계 기준 소요시간이 그대로 늘어나 "느려진 것"처럼 보이는 문제. 브라우저에서 이탈 자체를 물리적으로 막을 방법은 없으므로(웹페이지가 사용자의 자리 비움을 강제할 수 없음), 대신 무입력이 감지되면 화면을 멈춰 더 이상 시간이 왜곡되지 않게 하고, 쉰 시간을 투명하게 기록해 "총 시간"과 "실제 입력 시간"을 분리 — 논문에서 어느 쪽을 쓸지, 혹은 이상치를 어떻게 다룰지 연구자가 나중에 판단할 수 있게 원자료를 남김. 기준을 처음엔 5초로 뒀다가, 초급 학습자가 자판에서 글자 위치를 찾느라 5초 넘게 들여다보는 것도 정상적인 학습 행동이라 그 시간까지 "이탈"로 잘라내면 오히려 "찾는 시간이 줄어드는 것"이라는 진짜 향상 신호가 왜곡될 수 있음을 확인 → 20초로 상향 |
| 기본자리에 자모 반복 후 음절·낱말 조합 연습 추가 (마/머/니/리 → 머리/나라 등), 정답 시 발음 재생(Web Speech API) | 개별 자모 위치를 익히는 것과 실제 한글 단어를 조합해보는 것은 다른 기술. 배운 자모(ㅁㄴㅇㄹㅓㅏㅣ)로 만들 수 있는 실제 음절·단어를 연습시켜 위치 학습을 실제 타이핑 감각으로 연결. 발음 재생은 청각 피드백으로 동기 강화 |
| 음절·낱말 조합 연습을 기본자리 전용에서 전 단계(2~7단계)로 확장, 각 단계는 그 단계까지 "누적으로" 배운 자모만 사용 | 기본자리 이후 단계들은 새 자모만 단독으로 연습해서, 배운 자모들을 실제로 조합해보는 경험이 없었음. 각 단계 완료 시점엔 그 전 단계에서 배운 자모까지 다 알고 있으므로, 누적 자모로 만들 수 있는 실제 낱말(예: 왼손윗자리 완료 시점엔 "아버지", 가운데자리 완료 시점엔 게임 이름과 같은 "소나기")을 조합해보게 함. 겹받침·복합모음(ㅘㅙㅚ 등, 2벌식에서 키 2개를 조합해야 함)은 이 연습 범위에서 제외 |
| 기본자리(자모+조합) 완료 후 소나기 게임 자동 진입 대신 "더 연습하기/다음으로" 선택 제공 | 학습자마다 숙달 속도가 달라, 게임으로 강제 전환하기보다 스스로 판단해 반복 여부를 정하게 함 |
| 연습 화면에서 실시간으로 올라가는 "진행 시간(0.0초~)" 표시 제거 | 학습자(특히 초급 외국인 학습자)가 시간이 재지는 걸 보면 불안해할 수 있음. 시간 데이터 자체는 연구 목적상 계속 내부적으로 기록하되, 화면에는 노출하지 않음 |
| 완료한 단계는 로드맵에서 언제든 클릭해 재연습 가능하게 함 (진행 위치는 유지) | 학습자가 특정 자리에서 계속 헷갈리면 앞 단계로 돌아가 복습할 수 있어야 함. 복습 완료가 실제 진행 단계(다음 잠금해제 위치)를 되돌리지 않도록 별도 로직으로 분리 |
| Shift로 나오는 쌍자음·이중모음(ㅃㅉㄸㄲㅆㅒㅖ)을 별도 단계가 아니라 각자 base 키가 속한 기존 단계 안에 포함 (왼손윗자리·가운데자리·오른손윗자리) | 물리적으로 같은 키(Shift만 다름)이므로 굳이 새 단계로 분리할 이유가 없음. 키보드 다이어그램에 기본자모+Shift자모를 실제 키캡처럼 함께 표시하고, 현재 묻는 쪽을 강조해서 구분 |
| 로드맵에서 "실제 진행 위치(다음 잠금해제 단계)"와 "지금 화면에 보고 있는 단계"를 다른 스타일로 구분 (보는 중=진한 채움, 진행 위치=테두리만) | 완료한 단계를 복습하러 들어가면, 실제로는 다른(더 뒷) 단계가 여전히 진한 색으로 표시되어 "지금 이 단계를 하고 있다"고 착각하게 되는 문제가 있었음. 화면에 보이는 단계를 우선 표시하도록 수정 |
| (변경됨 — 아래 참고) 기본자리 완료 후 "일반 연습"과 "게임으로 연습" 중 선택하게 하고, 게임은 완료 후에도 로드맵에서 언제든 다시 실행 가능하게 함 | 게임은 반사신경 위주 활동이라 모든 학습자에게 강제하기보다 선택지로 제공. 그리고 한 번으로 끝내지 않고 원할 때 반복 연습할 수 있어야 함 |
| 위 결정을 뒤집음: "일반 연습/게임으로 연습" 중 고르는 화면을 없애고, 어느 단계를 누르든(처음이든 다시 보든) 항상 일반 연습부터 시작하게 함. 게임은 그 단계의 일반 연습(자모+조합 낱말)을 다 마쳐야 자동으로 이어짐 — 연습을 건너뛰고 게임만 골라서 할 수는 없음 | 게임을 전 단계로 확장(위 항목)하면서, 선택 화면이 있으면 학생이 실제 타이핑 연습(자모 위치 암기)을 건너뛰고 게임만 반복할 수 있는 구멍이 생김. 이 도구의 목적은 게임이 아니라 자판 위치 습득이므로, 게임은 항상 "연습을 마친 보상"으로만 오게 함 |
| 소나기 게임에 난이도 단계 부여 — 초반 15초는 자모만·느리게, 이후 조합 음절/단어(빨간색 표시)가 섞이며 속도 증가, 조합 단어를 잡으면 발음 재생 | 처음부터 어렵게 시작하면 게임 자체에 대한 진입장벽이 생김. 점진적으로 난이도를 올려 몰입을 유지하고, 조합 단어 발음 재생으로 게임 중에도 문자-소리 연결을 강화 |
| 게임에는 시간 제한이나 순위표(리더보드)를 두지 않음 | 시간 제한은 초급 외국인 학습자에게 불필요한 압박이 될 수 있어 보류. 순위표는 논문 연구 내용과 직접 관련이 없고 참여자 익명성 유지 취지와도 맞지 않아 추가하지 않기로 함 |
| 소나기 게임을 기본자리 전용에서 전 단계(1~7단계)로 확장 | 반복 연습이 지루해지지 않도록, 자모 암기 위주 연습 사이사이에 게임으로 긴장을 환기. 각 단계는 그 단계 자신의 자모·조합 낱말로 게임을 진행(예: 가운데자리 게임엔 가운데자리 자모만 등장). 3음절 넘는 낱말(예: 전체자리의 "고려대학교")은 게임 입력창의 오타 자동삭제(3자 이상 누적 시 초기화) 때문에 다 치기 전에 지워져버려 게임에서는 제외하고, 차분히 치는 조합 연습 화면에만 남김 |
| 게임을 "소나기 게임"에서 "단어비"로 개편: 잡는 방식을 입력 즉시 판정 대신 입력 후 스페이스바로 제출하는 방식으로 바꾸고, 느리게/보통/빠르게 속도 선택 UI 추가, 기본 낙하 속도를 더 느리게 하향, 정타 시 물방울 터지는 듯한 팝 애니메이션 추가 | 즉시 판정 방식은 연습 화면의 "스페이스바로 확정" 관습과 달라 일관성이 떨어졌고, 외국인 초급 타자 학습자에게는 기존 속도가 부담스러울 수 있어 속도를 직접 고를 수 있게 함. 떨어지는 낱말은 그 단계에서 배운 어휘(STAGE_COMBO) 위주로 구성해 "그 단계 것만 나온다"는 원칙 유지. 띄어쓰기 낱말(8단계)은 스페이스바가 제출 키가 되면서 게임에서 입력할 방법이 없어 게임 풀에서만 제외(조합 연습 화면엔 그대로 유지). 8~10단계는 새 자모가 없어 게임 낱말 풀을 조합 낱말로만 구성 |
| 단어비 전용 대량 어휘 풀(단계별 최대 40개, 그 단계까지 배운 자모만 사용) 추가, 매판 그중 15~20개만 무작위로 뽑아 고정된 큐로 순서대로 떨어뜨림(중복·연속 중복 원천 차단), 큐를 다 소진하면 "완료"로 게임 종료 | 40개를 한 판에 다 쏟아부으면 한 단계 클리어에 너무 오래 걸려 성취감이 떨어짐 — 1~2분 내외로 가볍게 끝내되 매판 다른 낱말 조합이 나오게 함. 1단계(기본자리)는 자음 4개+모음 3개, 받침 없음이라는 제약상 실제 존재하는 낱말이 14개가 한계라 40개를 못 채움 — 사용자와 상의 후 있는 만큼만 정직하게 채우기로 함(가짜 단어로 억지로 채우지 않음). 어휘 풀을 그 단계까지의 누적 자모·받침/이중모음/겹받침 도입 시점과 대조하는 검증 스크립트를 만들어 전수 검사했고, 이 과정에서 8단계 도입 전인 6·7단계 조합 연습 목록에 받침 있는 낱말("그림","한국어" 등)이 섞여 있던 기존 버그도 함께 발견해 수정 |
| 0단계에 8~10단계(받침·띄어쓰기 / 이중모음 / 겹받침) 추가. 새 물리 키가 없으므로 자모 드릴 없이 곧장 조합 연습으로 시작. 한글 분해기를 확장해 복합모음·겹받침을 2회 입력으로 정확히 분해, 조합 완성 순간(받침이 붙거나 이중모음·겹받침이 완성되는 순간) 짧은 팝 애니메이션으로 시각 피드백 | 1~7단계는 "어느 자판에 어떤 자모가 있는지"만 다뤘고, 한국어 타자의 핵심인 모아쓰기(자음+모음+받침을 한 글자로 묶는 원리) 자체를 다루지 않았음. 외국인 학습자는 이 조합 원리 자체가 낯설어서, 조합이 완성되는 순간을 시각적으로 보여주는 게 인지 부하를 줄이는 데 중요하다고 판단. 9단계 스펙 원안의 단독 자모 'ㅝ'는 2벌식에서 초성 없이 모음만 치면 IME가 자동으로 ㅇ을 채워 실제로 '워'로 조합되어 목표를 'ㅝ' 그대로 두면 영원히 일치할 수 없는 문제가 있어 '워'로 대체 |
| 연습 화면 어디서든 "처음으로" 링크로 시작 화면 복귀 가능하게 함 (단, 임의 단계로 바로 건너뛰는 자유 이동은 허용하지 않음) | 학습자가 다른 학생 계정으로 바꾸거나 처음부터 다시 확인하고 싶을 때가 있음. 다만 순차적 학습 설계 취지를 지키기 위해, 완료하지 않은 단계로 임의 이동하는 것까지는 허용하지 않음 |
| 참여자 식별은 실명 대신 참여자 코드 사용, 분반+이름 선택 방식으로 전환(직접 코드 입력 폐지) | 연구윤리(익명성 보장) + 직접 입력 시 오타·비일관적 코드로 데이터 분석이 어려워지는 문제 방지. 실제 출석부(학번 포함)를 별도 `students` 테이블로 관리하고, 연구 데이터(stage0_progress/records)에는 코드만 남게 해서 익명성 유지. 학번은 정정기간에 명단이 바뀌어도 변하지 않는 대조 키로 사용 — 이름 선택 후 학번 전체 입력으로 본인 확인(다른 학생 이름 오선택 방지) |
| 데이터 저장을 Supabase(PostgreSQL)로 전환 | 브라우저 로컬 저장(window.storage)은 참여자 접속 환경에 따라 불안정 — 중앙 DB로 참여자별 진행시간·정확도·오타 수를 안정적으로 수집 |
| 진행 상태 불러오기 실패 시 "신규 학생"으로 취급해 진행하는 대신, 화면을 막고 에러를 보여준 뒤 재시도하게 함. 저장 시에도 기존보다 낮은 진행도는 쓰지 못하게 이중 방어 | 불러오기 실패(네트워크 오류, Supabase 일시정지 후 재기동 등)를 "기록 없음"과 같은 것으로 취급하면, 이미 여러 단계를 마친 학생이 처음부터 다시 시작하게 되고 그 상태로 저장되는 순간 실제 진행 기록이 통째로 사라짐 — 실제로 참여자 1명에게 발생했던 사고를 계기로 발견·수정. 연구 데이터 무결성에 직결되는 문제라 우선순위를 높여 즉시 수정 |
| 무료 인프라(Vercel/Supabase 무료 플랜) 사용 + 자동 keep-alive 워크플로우 | 60명 규모 트래픽은 무료 플랜으로 충분. 단, Supabase 무료 플랜은 7일간 요청이 없으면 자동 일시정지되므로, GitHub Actions로 3일마다 자동 ping을 보내 연구 기간 내내 중단 없이 운영되도록 함 |
| 1주차(자판 자리 연습)와 2~3주차(성찰적 글)·4~5주차(요약하기)·6~15주차(보고서 쓰기)는 서로 독립된 별개 프로그램 — 하나를 마쳤다고 자동으로 다음 것으로 이어지지 않음 | 처음엔 1주차 완료 화면에 "다음 단계로 이동" 버튼(심지어 이미 폐지된 week1_hangul_typing.html을 가리키고 있었음)을 뒀는데, 사용자가 "자판 자리 연습이랑 나머지 성찰·요약·보고서랑은 다 별도야, 이어지는 게 아니야"라고 명확히 함 — 각 프로그램은 강의계획서의 해당 주차에 맞춰 독립적으로 접속하는 것이 맞고, 억지로 연쇄시키면 오히려 설계 의도(각 단위가 그 주차 강의 지문과 연계된 별개 도구)를 왜곡함. 그 버튼 제거 |
| 완료 후 "다시 연습하기"는 진행 상태(완료 단계·잠금 해제 위치)를 초기화하지 않고, 그냥 로드맵 화면으로 보내 원하는 단계를 자유롭게 골라 반복하게 함 | 기존엔 이 버튼이 진행 상태를 1단계로 되돌려서, 이미 전 단계를 마친 학생도 로드맵이 다시 잠기고 순서대로 처음부터 풀어야만 뒷단계에 접근할 수 있었음(자유 선택 불가). 사용자 피드백("모든 단계를 자유롭게 원하는 거 선택해서 할 수 있도록") 반영 — 로드맵의 완료(✓) 칩은 원래도 자유 클릭이 가능했으므로, 진행 상태를 건드리지 않기만 하면 자연스럽게 자유 선택이 됨 |
| 완료 요약 화면 표에도 그 단계를 몇 회 연습했는지(N회 배지) 표시 | 사용자가 "1회인지 2회인지, 회차마다 얼마나 빨라졌는지 봐야 발전이 있었는지 알 수 있다"고 지적. 회차별 상세(날짜·정확도·속도·추세)는 이미 "내 기록 보기"에 있었으므로(합산이 아니라 전체 이력을 회차 번호와 함께 이미 보여주고 있었음), 요약 화면에는 가볍게 "N회" 배지만 추가하고 상세는 "내 기록 보기"로 안내 |
| 교재에서 발췌한 저작권 있는 지문(2~3주차 핵심 개념어·어휘·문장)은 새 HTML 파일에 하드코딩하지 않고, Supabase 테이블(`week23_content`)에 넣어 앱이 실행 시점에 fetch로 가져오게 함 | 기존 `강의 자료 및 정보/`·`선행 연구 논문/`처럼 저작권 있는 원문을 공개 GitHub 소스코드에 그대로 남기지 않기 위함. 사용자에게 "Supabase 테이블 vs HTML 하드코딩" 두 방식을 제시했고 Supabase 테이블 방식을 선택함(추천안) |

## 5. 지금까지 한 작업 (진행 로그)

1. 1주차 문장 타이핑 프로토타입 제작 (`week1_hangul_typing.html`) — 9개 문항, 문항별 시간/정확도/분당타수/수정횟수 자동 기록
2. 0단계 자리연습 설계 방향 전환 확정 (자판 위치 자체를 모르는 학습자를 위한 사전 단계 필요) → 실제 구현 (`stage0_jari_practice.html`)
   - 6단계(기본자리/왼손윗자리/오른손윗자리/왼손아랫자리/오른손아랫자리/전체자리) + 소나기 게임(기본자리 완료 후) + 보너스 "가운데자리" 단계
3. Supabase 프로젝트 연동 — 참여자별 진행 상태·기록 저장, RLS 정책 설정
4. GitHub 저장소 생성 + Vercel 배포, 랜딩 페이지(`index.html`)로 전체 프로그램 로드맵 통합
5. Supabase 무료 플랜 자동 일시정지 대비용 GitHub Actions 예약 작업 추가
6. 디자인 톤 점검: 참고 이미지(보라색 계열) 검토 후, 연구도구 중립성을 이유로 기존 종이/teal 팔레트 유지하되 명확성(버튼 크기·대비) 강화
7. 실제 2026-2학기 강의계획서를 확인해 "2주차 이후" 콘텐츠 연계 구조를 4단계(1주차/2~3주차/4~5주차/6~15주차)로 확정, `index.html`·`project-brief.md`에 반영
8. 참여자 식별 방식을 코드 직접입력 → 분반/이름 선택(+학번 본인확인)으로 전환, 실제 출석부(C3 30명, C4 13명) 기반 `students` 테이블 구축, 미등록 학생 자기등록 기능 추가
9. 0단계를 6단계+보너스 구조에서 7단계(기본자리/왼손윗/왼손아래/가운데자리/오른손윗/오른손아래/전체자리)로 재구성, 가운데자리를 정규 단계로 승격
10. 자모 반복 방식을 "연속 3회"에서 "순서 도입 1회 + 무작위 반복 2회"로 변경, 스페이스바 진행 확인 방식 도입
11. 기본자리에 음절·낱말 조합 연습(발음 재생 포함) 및 연습 완료 후 "더 연습/다음으로" 선택 단계 추가
12. 실시간 진행 시간 표시 제거(데이터는 내부 기록 유지), 완료 단계 자유 재연습 기능 추가
13. 로드맵 칩 상태를 "보는 중/진행 위치"로 구분, 연습 화면 어디서든 "처음으로" 복귀 링크 추가
14. 소나기 게임에 단계별 난이도(자모→조합 음절/단어, 속도 증가)와 조합 단어 발음 재생 추가, 기본자리를 로드맵에서 게임으로 언제든 재도전 가능하게 함
15. 실제 키보드 형태(숫자열·Tab·Caps·Enter·Shift·Ctrl·Alt 포함)로 자판 다이어그램 전면 개편, 연습 화면·랜딩 페이지 전반 확대(가독성 개선)
16. 스페이스바 진행 확인 방식을 폐지하고 정답 시 자동 진행으로 복귀, 오답 시 비프음 피드백 추가. Shift가 필요한 항목은 반대손 Shift 키도 자판 다이어그램에서 함께 강조
17. 기록(내 기록 보기) 화면에 같은 단계의 반복 회차·속도(글자수/시간)·직전 회차 대비 추세(▲▼) 표시 추가
18. 무입력 20초/탭 전환 감지 시 자동 일시정지(이탈 시간 분리 기록) 기능 추가 — `stage0_records`에 `idle_count`/`idle_total_sec` 컬럼 추가(마이그레이션 SQL은 `supabase_setup.sql`에 반영, Supabase에서 실제 실행 완료)
19. 음절·낱말 조합 연습을 기본자리 전용에서 2~7단계 전체로 확장 (누적 자모 기반). 유니코드 공식 기반 한글 분해 함수를 새로 만들어 자모-키 매핑을 일반화
20. 소나기 게임을 기본자리 전용에서 전 단계로 확장 — 각 단계 완료 시 그 단계 자모로 게임 진행
21. 조합 연습(음절/낱말) 화면에서 오답 시 소리가 안 나던 문제 수정 (백스페이스로 고칠 때 소리 재생), AudioContext가 일시정지 상태로 남는 경우 대비 코드 추가
22. "일반 연습/게임으로 연습" 선택 화면 제거 — 게임을 건너뛰고 골라 할 수 없도록, 항상 일반 연습부터 시작하고 게임은 그 뒤에 자동으로 이어지게 함
23. "○○ 연습을 마쳤어요!" 체크포인트 화면에서는 무입력 자동 일시정지가 걸리지 않게 하고, 그 화면에 머문 시간은 해당 단계의 기록(시간·속도)에서 제외 — 연습 자체는 체크포인트 도달 시점에 이미 끝난 것으로 봄
24. 조합 연습(음절/낱말)에서 틀린 글자를 치고 계속 입력해도 지워지지 않던 문제 수정 — 마지막(아직 조합 중일 수 있는) 글자는 판정하지 않고, 그 앞의 "확정된" 글자들만 목표와 비교해서 어긋나면 확정 오답으로 지움
25. 정답 시 자동 진행을 다시 스페이스바 확인 방식으로 되돌림(엄지손가락으로 스페이스바를 눌러 손을 홈 포지션으로 되돌리는 습관 형성 목적). 스페이스바를 기다리는 중에도 무입력 자동 일시정지는 그대로 적용되고, 일시정지에서 돌아오면 다시 입력할 필요 없이 스페이스바만 누르면 되게 처리
26. 0단계에 8~10단계(받침과 띄어쓰기 / 이중모음 / 겹받침) 추가 — 한글 모아쓰기 원리를 다루는 심화 단계. 한글 분해기를 복합모음·겹받침까지 지원하도록 확장하고, 글자 조합이 완성되는 순간 팝 애니메이션으로 시각 피드백 추가. 총 10단계 구성으로 랜딩 페이지 설명 업데이트
27. 게임을 "소나기 게임"에서 "단어비"로 개편 — 스페이스바 제출 방식, 속도 선택(느리게/보통/빠르게) UI, 정타 시 물방울 팝 애니메이션 추가. 8~10단계에도 게임 활성화(조합 낱말로만 구성)
28. 단어비에 단계별 대량 어휘 풀(최대 40개) 추가, 매판 15~20개만 무작위로 뽑아 중복 없이 소진하는 방식으로 변경(다 뽑으면 "완료"로 종료). 자모 검증 스크립트로 전수 검사하며 6·7단계 조합 연습 목록의 받침 관련 기존 버그도 함께 수정
29. 0단계(자판 자리 연습)를 별도 "0단계"가 아니라 "1주차"로 재편입, 기존에 따로 있던 1주차 문장 타이핑(week1_hangul_typing.html)은 랜딩 페이지에서 제외
30. **[중요, 데이터 유실 버그 수정]** 진행 상태 불러오기(loadProgress)가 네트워크 오류·Supabase 일시정지 후 깨어나는 중 등 어떤 이유로든 실패하면 "진행 기록이 없는 신규 학생"과 똑같이 취급해 처음부터 다시 시작하게 되고, 그 상태에서 1단계를 다시 마치는 순간 실제 진행 기록이 통째로 덮어써지는 버그를 발견 — 실제로 참여자 한 명(C3-S861018, "소리나")에게 발생해 10단계까지 다 끝낸 기록이 1단계로 되돌아가 있었음. stage0_records(실제 완료 기록)를 근거로 진행 상태를 직접 복구했고, 나머지 모든 참여자의 기록도 전수 대조해 다른 피해자는 없음을 확인함. 재발 방지: 불러오기 실패 시 절대 "신규 시작"으로 처리하지 않고 화면을 막고 에러 표시 + 재시도 유도, 저장 시에도 기존 값보다 낮은 진행도를 못 쓰게 이중 안전장치 추가
31. 위 사고를 계기로 파일 전체의 Supabase 호출부(try/catch)를 전수 점검 — 진행 상태 저장(saveProgress)에 자동 재시도(최대 3회) 추가하고 그래도 실패하면 학생 화면에 경고 배너 표시(이전엔 콘솔에만 조용히 기록되어 학생은 저장 안 된 줄 모르고 계속 진행할 수 있었음), 기록 조회(fetchAllRecords)도 "기록 없음"과 "불러오기 실패"를 구분해서 보여주게 함. 나머지 호출부(자기등록 중복키 재시도, 명단 불러오기, 발음/효과음 재생)는 실패해도 데이터를 덮어쓰지 않는 것을 확인해 그대로 둠
32. 2~3주차(성찰적 글) 프로토타입 제작 (`week2_3_reflective_writing.html` + `supabase_setup_week23.sql`) — 교재 「외국인 유학생을 위한 학술적 글쓰기」 23~44쪽(3~5단원, 성찰적 글 계획·작성·수정하기)에서 핵심 개념어 8개·지문 어휘·표현 9개·핵심 문장 5개(모두 30쪽 학생 글 본문에서 발췌, 페이지 이미지와 대조해 원문 일치 확인)를 뽑아 3단계(핵심 개념어→지문 어휘·표현→핵심 문장) 순차 타이핑 프로그램으로 구현. 1주차와 동일한 학생 로그인(분반/이름/학번 확인)·저장소 안전장치(불러오기 실패 시 진행 차단, 저장 재시도+퇴행 방지, 경고 배너)를 그대로 재사용하되, 자판 위치 다이어그램·소나기(단어비) 게임은 이 단위의 목적(문맥 속 실제 글 타이핑)에 맞지 않아 넣지 않음. 콘텐츠(교재 원문)는 새 Supabase 테이블 `week23_content`에서 fetch — 불러오기 실패 시에도 "내용 없음"으로 조용히 넘어가지 않고 화면을 막음(1주차 데이터 유실 사고 이후의 원칙을 신규 파일에도 처음부터 적용). `supabase_setup_week23.sql`은 아직 사용자가 Supabase 대시보드에서 실행 전 — 실행 전까지는 콘텐츠 로딩이 의도대로 실패 처리됨을 확인함(정상 동작)
33. 1주차 완료 요약 화면의 "1주차 문장 타이핑으로 이동" 버튼(폐지된 week1_hangul_typing.html로 연결되어 있었음) 제거 — 프로그램 단위 간에는 자동 연계가 없다는 방향이 확정되어, 다음 단위로 넘기는 버튼 자체가 불필요해짐
34. "다시 연습하기" 버튼이 진행 상태(완료 단계·잠금 해제 위치)를 1단계로 초기화하던 로직 제거 — 이제는 진행 상태를 그대로 두고 로드맵 화면으로만 이동, 완료(✓) 단계 칩을 자유롭게 클릭해 원하는 단계를 바로 재연습 가능(1주차·2~3주차 동일 적용). 완료 요약 화면 표에 단계별 연습 회차 수("N회" 배지) 표시 추가 — 상세 회차별 비교는 기존 "내 기록 보기"(회차·속도·추세 ▲▼) 그대로 안내. 2~3주차 "내 기록 보기"도 1주차와 동일한 회차/추세 비교 방식으로 맞춤
35. `supabase_setup_week23.sql`을 Supabase 대시보드에서 실제 실행 완료 (`week23_content`/`week23_progress`/`week23_records` 테이블 생성, 콘텐츠 22행 삽입 — REST로 직접 조회해 확인). `index.html`의 "2~3주차 · 준비 중" 카드를 실제 링크(`week2_3_reflective_writing.html`)로 활성화, 아직 구현 안 된 "영·중 뜻 함께 제공" 문구는 설명에서 제거
36. **[타이핑 UX 버그 수정]** 조합 연습(낱말/문장) 중 오타를 내면 이미 맞게 친 부분까지 포함해 입력창 전체가 지워지던 문제 발견·수정 — 짧은 낱말은 다시 쳐도 부담이 적지만, 2~3주차의 긴 문장에서 끝부분 오타 하나로 전체를 다시 치게 하면 학습자가 크게 좌절함(실제 사용자가 문장 타이핑 중 직접 겪고 보고함). 틀리기 직전까지 맞았던 부분은 남기고 오타 이후만 지우도록 수정 — 1주차·2~3주차 동일 적용
37. **[2~3주차 어휘 선별 기준 전면 재설계]** 처음엔 개발자(어시스턴트)가 본문을 읽다가 임의로 고른 개념어·문장이었는데, 사용자가 "이 프로그램의 존재 이유가 외국인 학생이 한국어 교재를 미리 이해하게 하는 것"이라는 점을 지적 — 선별 기준을 "그 파트를 이해하는 데 필요한 어휘(난이도 무관)"로 재정의. 구조도 "1개 통합 단원(개념어→어휘→문장)"에서 교재의 실제 3단원 구성 그대로 **파트 1(계획하기)/파트 2(작성하기)/파트 3(수정하기)**로 전환(실제 수업도 이 3단위로 진행됨을 확인). 사용자가 파트 1(23~28쪽) 어휘를 직접 3그룹(① 단원 핵심 개념어 ② 글쓰기 활동 관련 필수 어휘=지시문 이해용 ③ 예시·마인드맵 이해용 어휘=서사 파악용)으로 총 23개 큐레이션 — 원문 대조로 정확성 확인(예: "객관적" 정의가 교재 문장 그대로임을 확인). 화면 표시명은 짧게 "핵심 개념어/활동 지시문 어휘/예시 이해 어휘"로 확정. 영어·중국어(간체) 번역 추가(수강생 국적 분포상 이 두 언어로 확정). 사람이 보기 쉬운 참고 정리본을 `week23_vocabulary.md`로 별도 작성(교재 파생 콘텐츠라 `.gitignore` 처리 — 실제 데이터는 DB에만)
38. 파트 1을 실제 앱에 반영 — `week23_content`에 `part` 컬럼 추가(파트 2·3 확장 대비), 콘텐츠를 위 23개로 전면 교체. 앱의 `STAGE_DEFS`를 3그룹(핵심 개념어/활동 지시문 어휘/예시 이해 어휘)으로 교체하고 "핵심 문장" 단계는 제거(3단원엔 완결된 읽기 지문이 없어 이 파트엔 안 맞음 — 파트 2에서 다시 쓸 예정). 뜻풀이 표시를 한국어(쉬운 설명)+영어+중국어 3단 동시 노출로 확장. **뜻풀이 공개 시점 설계**: "타이핑 전 바로 보여주기" vs "정답 후 공개"를 사용자와 논의 — 이 도구의 목적이 "이미 아는지 시험"이 아니라 "수업 전 사전 노출·이해"이고, 타이핑 자체는 이미 화면에 보이는 한글을 그대로 옮기는 동작이라 공개 시점을 늦춰도 타이핑 난이도엔 영향이 없고 이해만 늦어지므로, 즉시 공개 방식으로 확정. 랜딩 페이지 카드 설명도 "파트 1(계획하기) 제공, 파트 2·3 준비 중"으로 갱신. 파트 2·3(작성하기/수정하기)은 아직 착수 전
39. **[파트 2(작성하기, 4단원, 29~36쪽) 어휘 큐레이션]** 파트 1과 동일한 기준으로 어시스턴트가 초안을 먼저 뽑고 사용자가 검토·조정하는 방식으로 진행(파트 1은 반대로 사용자가 먼저 큐레이션). 핵심 개념어 13개(처음-중간-끝 구성, 독자·전달하다·비유·빗대다·단락 나누기·중심 문장·뒷받침 문장·삶으로의 적용·제목 등)를 확정. "활동 지시문 어휘" 그룹은 초안(나열하다/어울리다/유의하다/살펴보다/점검하다/생생하다)을 냈다가, 사용자가 "이게 정말 필요할까?"라고 지적 — 다시 보니 이 단원 고유 어휘가 아니라 교재 전체에 반복되는 범용 지시어라 판단해 그룹째 제외(파트 1의 "활동 지시문 어휘"는 그 단원 고유 절차 어휘라 남겨둠, 그룹 성격에 따라 파트마다 있을 수도 없을 수도 있음을 확인). "예시 이해 어휘" 대신 교재 자체 소제목 "읽어보기"를 그대로 써서 "읽어보기(30쪽) 어휘"로 명명(페이지 표시 — 핵심 개념어는 여러 쪽에 걸쳐 나와 페이지 표시 안 함). **완전성 검증**: 사용자가 "저 어휘만 알면 이 글을 다 이해할 수 있어?"라고 질문 — 문장 단위로 재대조한 결과 실제로 8개(속상하다·견디다·유독·다름없이·쬐다·홀로·펑펑·품다)가 누락되어 있었음을 발견, 애매한 5개(존재하다·한참·차분히·대하다·여전히)도 포함해 42개로 확정. "핵심 문장" 타이핑 단계(읽어보기 학생 글에서 비유 예문 5개)도 검토했으나, 문장 속 핵심 단어가 전부 "읽어보기 어휘"에 이미 있어 중복 연습이 되고 학생 피로도만 는다고 판단해 제외. 여러 파트에 같은 단어가 재등장하는 문제(다짐/무시하다/한없이)는 각 파트 본문 이해에 필요하면 중복 삽입하기로 확정 — 단, 통일성처럼 "이미 배운 개념이 유의사항으로 반복 언급"되는 경우는 중복 제외
40. 파트 2를 실제 앱에 반영 — `STAGE_DEFS`를 파트별 stage 5개(파트1: 개념어/활동어휘/예시어휘, 파트2: 개념어/읽어보기어휘)로 확장하고, 콘텐츠 조회 키를 `stage_key` 단독에서 `part:stage_key` 복합키로 변경(같은 stage_key가 파트마다 다른 내용을 가리키므로). 로드맵에 파트 구분 라벨("파트 1 · 계획하기" 등)을 추가해 5개 칩이 어느 파트 소속인지 시각적으로 구분. 파트 1을 다 마쳐야 파트 2가 열리는 순차 잠금 구조(실제 수업이 계획하기→작성하기 순으로 진행되는 것과 동일). **[콘텐츠 로딩 버그 발견·수정]** 파트 2 콘텐츠 반영 직후 실제 화면에서 "학습 내용을 불러오지 못했습니다" 에러가 재현됨 — DB엔 데이터가 정확히 다 있는데도 실패. 원인 확인 결과 `loadContent()`가 페이지 로드 시 단 한 번만 시도하고 실패해도 재시도가 없어서, SQL을 막 실행한 직후처럼 Supabase가 일시적으로 지연되는 순간에 딱 걸리면 그 세션 내내 영구히 실패 상태에 갇히는 구조적 문제였음(`saveProgress`엔 이미 있던 자동 재시도가 `loadContent`엔 없었음). `loadContent()`에도 동일한 자동 재시도(최대 3회, 지수 백오프)를 추가해 해결·검증 완료
41. **[2~3주차 화면 구조 개편: 파트 선택 화면 도입]** 로그인 후 5개 단계 칩이 파트 구분 없이 한 화면에 다 보여서 복잡하다는 피드백 — 로그인 후 "파트 선택" 화면(파트 1/2/3 카드, 상태를 진행 중/완료/잠김/준비 중으로 표시)을 새로 만들고, 각 파트에 들어가면 그 파트의 단계만 로드맵에 표시하도록 변경. 단계 완료 후 다음 단계가 다른 파트로 넘어가는 시점(또는 전체 복습 완료 시점)엔 자동으로 다음 파트로 진행하지 않고 파트 선택 화면으로 돌려보내 학생이 직접 고르게 함. "다시 연습하기"도 파트 선택 화면으로 가도록 통일. 전체 진행 현황(집계 통계) 화면은 파트 선택 화면의 별도 버튼으로 이동
42. **[파트 3(수정하기, 5단원, 37~44쪽) 어휘 큐레이션]** 원문을 다시 분석한 결과, 이 파트는 새 서사 지문이 필요 없다는 특이점 발견 — "조사 바르게 사용하기" 연습이 파트 2 학생 글 문장을 그대로 재사용(오류만 삽입)하고, "부사 적절하게 사용하기" 연습 단어 5개(펑펑·홀로·유독·한없이·다름없이)도 전부 파트 2 읽어보기 어휘와 동일. 어시스턴트가 초안(핵심 개념어 6개)을 먼저 제시했고, 사용자가 실제 체크리스트 이미지 2장(주제 및 내용/단락 및 표현/제목 체크리스트, 큰 범위→작은 범위 순서 체크리스트)을 스크린샷으로 제공하며 대조 확인 요청 — 이 과정에서 수정하다·주제·내용·문법·어휘·띄어쓰기·문장 부호·범위·순서·분위기 10개를 추가 발굴. 특히 **문장 부호**는 예전부터 "꼭 챙기기"로 남아있던 미정 항목이었는데 이번에 반영됨. 제목·구체적·강조·상황·단락(단독)은 이미 배운 개념의 재언급이거나 다른 단어 정의에 쓰인 것으로 판단해 제외. 부사 5개는 "읽기 이해"(파트2)와 "정확히 골라 쓰기"(파트3) 목적이 달라 중복 수록하기로 확정 — 최종 핵심 개념어 16개 + 부사 연습 어휘 5개로 확정
43. 파트 3을 실제 앱에 반영 — `STAGE_DEFS`에 파트 3 stage 2개(핵심 개념어/부사 연습(42쪽) 어휘) 추가, `PART_DEFS`의 파트 3 `ready`를 `true`로 전환. `week23_vocabulary.md`·`논문자료/어휘_선정_기록.md`·`논문자료/개발_과정_기록.md` 모두 파트 3 반영해 갱신 — 이로써 2~3주차(성찰적 글) 파트 1·2·3 어휘 선정·구현이 모두 완료됨
44. 파트 선택 화면의 파트 카드 제목을 "계획하기"처럼 짧게 쓰던 것에서 교재 원래 단원 번호·제목 그대로("3. 성찰적 글 계획하기", "4. 성찰적 글 작성하기", "5. 성찰적 글 수정하기")로 변경. 파트 2의 "읽어보기(30쪽) 어휘" 그룹명도 "읽어보기(30쪽) 어휘와 표현"으로 변경(단어뿐 아니라 "안부를 묻다" 같은 표현·구절도 섞여 있어 더 정확한 이름) — `week23_vocabulary.md`·`논문자료/어휘_선정_기록.md`도 함께 갱신
45. 잠긴 파트를 미리보기할 수 있게 해달라는 요청이 있었으나, 사용자가 질문에 응답하지 않고 보류 — 아직 미착수(§6 참고)
46. **[중요, 조합 오답 판정 버그 재발 방지]** 사용자가 실사용 중 오타 없이 정확히 입력했는데도 이미 확정된 글자가 사라지는 문제를 재보고("배경" 타이핑 중 "배"가, "암시하다" 타이핑 중 "암"이 사라짐) — 지난번(§36) 수정한 "오답 시 부분 삭제" 로직만으로는 부족했음이 드러남. 실제 원인은 오답 판정이 아니라 **정답 판정**이었음: 한글 IME가 아직 조합 중이라 최종 확정되지 않은 문자열이 우연히(또는 받침이 아직 다음 글자로 넘어갈지 결정되지 않은 시점에) 목표 문자열과 같아 보이는 순간, 즉시 입력창을 비활성화(`disabled=true`)해 버려서 그 타이밍에 브라우저의 IME가 조합 상태를 잃고 이미 확정된 앞 글자까지 함께 날아가는 것으로 추정됨. 브라우저가 실제로 제공하는 조합 상태 신호(`InputEvent.isComposing`)를 확인해 **조합이 완전히 끝나기 전에는 정답·오답 판정 자체를(그리고 그에 따른 입력창 비활성화도) 미루도록** 정답 판정부까지 수정 — 오답 판정에서만 이 가드를 걸었던 지난번 수정의 사각지대를 메움. 조합 종료 후에도 값이 그대로면 별도 input 이벤트가 안 오는 브라우저가 있을 수 있어 `compositionend`에서 합성 input 이벤트를 한 번 더 보내는 안전망도 추가. 합성 이벤트로 "정답이 이미 조합 중일 때는 판정을 미루고, 조합 종료 후에만 판정된다"는 시나리오를 실제로 재현해 수정을 검증함. 1주차·2~3주차 동일 적용
47. 학생이 "처음으로"를 눌러 로그인 화면으로 나갔다가 다시 들어올 때마다 분반·이름·학번을 매번 새로 입력해야 해서 번거롭다는 피드백 — 이번 세션에 이미 학번 확인을 마친 학생이면(currentCode가 남아있으면) 로그인 화면에 폼 대신 "OO님, 이어서 진행하시겠어요?" + "계속하기"(재확인 없이 파트 선택으로 바로 이동) + "다른 학생으로 로그인"(폼을 다시 보여줌) 버튼을 보여주도록 개선
48. **[번역 품질 검증 — AI 교차 검토]** 사용자가 "띄어쓰기"의 중국어 번역("分写")이 부정확한 것 같다고 지적 — 실제로 일반 유학생에게는 생소한 학술 용어였음, "空格"(사용자 확정)으로 수정. 이를 계기로 전체 99개 항목을 재검토해 "객관적"의 중국어 번역이 형용사형("客观的")이어야 하는데 부사형("客观地")으로 잘못돼 있던 것도 함께 발견·수정. 어시스턴트 자신의 재검토만으로는 한계가 있다고 판단해, 사용자가 직접 다른 생성형 AI(GPT)에게 교재 원문(PDF)과 검토용 표(`논문자료/검토용_어휘표.md`, 99항목)를 함께 주고 재검토를 요청함(프롬프트는 어시스턴트가 작성). 결과를 `.docx` 파일로 받아 반영 — 6개 항목(객관적·관점·처음-중간-끝·속상하다·주저앉다·펑펑) 수정, 9개 항목(들여다보다·눈물을 머금다·띄어쓰기·다름없이·마음을 속이다·아깝다·비유·삶으로의 적용·주제문)은 "원안 유지"로 확인받음. 특히 "띄어쓰기=空格"는 어시스턴트가 우려했던 것과 달리 이 교차 검토에서 문제없다고 재확인됨(직관적이라 명백한 오역 아님). SQL·`week23_vocabulary.md`·`논문자료/어휘_선정_기록.md`(§8.1로 검토 과정 기록) 모두 반영 완료, 실제 Supabase DB에도 수정 SQL 실행 완료(REST로 재조회해 확인) — **중국어 원어민의 최종 감수만 별도로 필요**
49. **[이해도 확인 퀴즈 구현 — RQ1 측정 도구]** §2에서 연구문제를 최종 확정하면서 RQ1(사전 내용 이해도)을 실제로 측정할 도구가 없다는 점을 인식 — 2~3주차(성찰적 글) 프로그램에 파트를 처음 완료했을 때(복습 중엔 제외, 데이터 중복·번거로움 방지) 그 파트 어휘에서 자동 출제되는 4지선다 이해도 확인 퀴즈를 추가. 파트당 최대 8문제, 단어→뜻/뜻→단어 방향을 무작위로 섞고, 오답 선택지는 같은 그룹(핵심 개념어 등) 안에서 우선 뽑아 난이도를 통제(그룹 내 오답 후보가 3개 미만이면 같은 파트의 다른 그룹에서 보충) — 기존에 이미 큐레이션해 둔 어휘 데이터(`contentByStage`)를 그대로 재사용해 새 콘텐츠 제작 없이 구현. 결과는 새 테이블 `quiz_results`(unit/part/participant_code/score/total/문항별 상세 JSON)에 저장 — `unit` 필드를 둬서 4~5주차 등 향후 프로그램에도 같은 구조 재사용 가능. 디버그 훅으로 실제 로드된 어휘 데이터를 대상으로 문제 생성→답변 클릭→채점→결과 화면→저장까지 전 과정을 브라우저에서 직접 실행해 검증 완료. `supabase_setup_week23.sql`에 `quiz_results` 테이블 생성 SQL 추가 후 Supabase에서 실행 완료(REST 조회로 확인)
50. **[퀴즈 노출 조건 버그 수정]** 사용자가 실사용 중 "퀴즈가 안 나온다"고 보고 — 원인은 퀴즈 노출 조건이 "이번이 처음 완료냐(isForward)"였는데, 사용자 계정(C3-S861018)은 퀴즈 기능이 추가되기 *전에* 이미 파트 1·2를 끝내둔 상태라 다시 들어가도 항상 "복습"으로 취급돼 영원히 퀴즈를 볼 기회가 없었음. 노출 조건을 "처음 완료했는지"가 아니라 "이 참여자의 이 파트 `quiz_results` 기록이 이미 있는지"로 변경 — 이렇게 하면 퀴즈 기능 추가 이전에 이미 끝내둔 파트도 한 번은 보게 되고, 그 이후 복습부터는 건너뜀. 실제 참여자 데이터(파트 1·2=기록 없음→노출 예정, 파트 3=기록 있음→건너뜀)로 판정 결과를 직접 확인해 검증
51. **[파트 2 "활동 지시문 어휘" 그룹 누락 발견·보완]** 사용자가 "파트 1엔 활동 지시문 어휘가 있는데 파트 2엔 왜 없냐"고 일관성 문제 제기 — 처음엔 "파트 2 지시문이 이미 핵심 개념어를 재사용해서 구조적으로 불필요하다"고 추론했으나, 이는 원문을 직접 재확인하지 않은 부정확한 답이었음(사용자가 "정확해?"라고 재확인 요청해 발견). 교재 29~36쪽 원문을 다시 대조한 결과, 예전에(§39) "범용 지시어라 4단원 고유 어휘가 아니다"라고 판단해 제외했던 초안 6개 중 4개(나열하다·어울리다·유의하다·살펴보다)가 실제로 이 단원 지시문(31·34쪽)에 그대로 쓰이고 있음을 확인 — 나머지 2개(점검하다·생생하다)는 이 페이지 범위에서 확인 안 돼 제외 유지. 사용자가 "학생들이 잘 모를 것 같다"고 판단해 4개를 파트 2 "활동 지시문 어휘" 그룹으로 신규 반영. 이 그룹 추가로 `STAGE_DEFS`의 전체 단계 번호가 한 칸씩 밀려나서(7단계→8단계) 1회성 마이그레이션 SQL을 준비했으나, **실행하기 전에 사용자가 "SQL 올리기 전에 어휘 최종 검토부터 하자"고 요청** — 아직 실제 DB에는 반영 전(§52에서 이어짐)

52. **[검토 중 "활동 지시문 어휘" 그룹 자체를 파트 1·2 모두에서 최종 제외]** SQL 반영 전 최종 검토를 위해 파트 1·2·3 전체 어휘(103개)를 한눈에 볼 수 있는 검토용 Artifact 페이지를 만들어 제공, 신규 추가된 4개를 NEW로 표시. 검토 중 사용자가 몇몇 단어(무시하다·배경·방향·눈물을 머금다·바람·무너지다)의 뜻풀이를 사전적 의미 기준으로 더 쉽게 다듬어 달라고 요청해 6개 반영(SQL·`week23_vocabulary.md`·`논문자료/어휘_선정_기록.md`·검토 Artifact 모두 갱신) — 이 과정에서 "무시하다"의 "(교재 맥락)"이라는 괄호 설명이 학생 화면에 그대로 노출되는 걸 알아채고, 괄호 없이도 자연스러운 문장으로 다시 씀. 이어서 사용자가 "활동은 어차피 내가 수업 시간에 설명해주는데, 활동 지시문 어휘 자체가 필요할까?"라는 근본적인 질문을 제기 — 세 가지 선택지(전부 유지 / 파트 2 신규분만 제외 / 파트 1·2 모두 제외)를 제시했고, 사용자가 **파트 1·2 모두 제외**를 선택함. 판단 근거: 핵심 개념어·읽어보기 어휘는 학생이 혼자 텍스트만으로 이해해야 하는 반면, 활동 지시문은 원래 교수자의 실시간 설명이라는 안전망이 있어 사전학습 도구로 다룰 실익이 낮고, "활동 수행 절차 이해"가 RQ1("수업 지문 내용 이해도")이 재려는 것과도 결이 다름. 파트 1의 기존 6개(바탕·관점·태도·확장하다·구체적·선정하다)와 파트 2의 신규 4개(나열하다·어울리다·유의하다·살펴보다)를 그룹째 모두 제외 — `STAGE_DEFS`가 최종적으로 6단계(원래 라이브 DB의 7단계에서 활동 그룹만 제거된 상태)로 재구성됨. `supabase_setup_week23.sql`·`week23_vocabulary.md`·`논문자료/어휘_선정_기록.md`(§9를 "그룹 추가와 최종 제외" 전체 경과로 재작성) 모두 반영, 실제 참여자(C3-S861018)의 기존 진행 기록(파트1 활동어휘 완료기록 2건 포함)을 보존하면서 새 6단계 번호로 옮기는 마이그레이션 SQL 준비 — **아직 실제 DB에는 미실행, 사용자 확인 후 실행 예정**
53. **[SQL 실행 완료 + 이중 실행 사고 발견·복구]** 사용자가 마이그레이션 SQL을 실행했는데, 대시보드에서 Run을 두 번 누른 것으로 추정되는 이중 실행이 발생 — `week23_progress.completed_stages`에서 값 하나(2번, 예시 이해 어휘 완료 표시)가 통째로 사라지고, `week23_records`의 여러 기록이 실제보다 한 단계씩 더 밀려 있었음(예: "예시 이해 어휘" 기록이 "활동 지시문 어휘" 기록과 같은 902번에 섞여버림). REST로 직접 데이터를 조회해 문제를 정확히 진단(이중 함수 적용으로 역산 가능했음) → 복구 SQL(진행 상태는 참여자 1명뿐이라 값을 직접 명시적으로 지정, 기록은 stage_name으로 두 그룹을 구분해 분리 복구)을 작성해 실행, REST 재조회로 완전 복구 확인. 이후 `supabase_setup_week23.sql` 본 실행도 완료 — 활동 지시문 어휘 콘텐츠 삭제(0건), 전체 콘텐츠 93개, 뜻풀이 수정 6개 모두 반영 확인. "바람" 항목은 검토 중 사용자가 "동음이의어라는 말 대신 두 뜻을 다 명시해야 할 것 같다"고 추가 요청해 "①공기의 움직임 ②이루어지기를 바라는 마음, 여기서는 ②" 형태로 재작성(SQL·문서·검토 Artifact 모두 반영)
54. **[프로그램 범위 최종 확정: 3개 파트, 보고서 쓰기 제외]** 사용자가 "1.자판자리 연습 2.성찰적 글 3.요약하기, 이렇게 3개로 하고 보고서는 빼겠다"고 범위를 최종 결정 — 사유: 보고서 쓰기(6~15주차, 유형→주제→참고문헌→개요→서론→인용→조사→결론→수정)는 제작 분량이 지나치게 크고, 이미 만든 단위 2개만으로도 도구 효과를 보여주기 충분해 세 번째 단위를 더 만드는 것은 "결국 반복"이라 판단(§3에 상세 반영). `index.html`에서 "6~15주차 보고서 쓰기" 카드 제거. 이어서 사용자가 "대신 2·3(성찰적 글·요약하기)을 연구 주제·문제에 맞춰 밀도 있고 탄탄하게 구성해야겠다"는 방향을 제시 — 구체적 실행 항목은 §6에 기록, 바로 이어진 §55가 그 첫 실행 사례
55. **[RQ1 측정의 구성 타당도 재검토 → 지문 읽기 화면 추가]** 사용자가 "지금 완성한 성찰적 글 프로그램이 연구 주제·연구문제를 잘 담았는가"를 직접 질문 — 어시스턴트가 솔직하게 평가한 결과, 두 가지 한계를 짚음: (1) 지금 퀴즈는 "어휘 재인"만 재고 "내용 이해"까지는 못 잼 (2) RQ1이 "영향"(효과)을 묻는데 비교 기준(사전/대조군) 없는 사후 단일 측정이라 인과 주장이 약함. 1번부터 해결 방안을 논의하던 중, 사용자가 핵심 질문을 던짐 — "어휘 타이핑만으로 파트 2 지문 전체를 다 이해할 수 있어?" 이에 대해 정직하게 답함: 아니오, 지금 도구는 42개 어휘를 각각 따로(문맥·문장 연결 없이) 타이핑하게 할 뿐이라 문법·담화 흐름까지 익히긴 어려움 — 즉 "완전성 검증"(§3 항목39)은 "이 단어들만 알면 이해 가능하다"는 **가설**이었지 검증된 사실이 아니었음. 이 상태에서 내용 이해 문항을 추가하면 "학생이 틀린 게 도구 부족 때문인지, 애초에 어휘만으론 안 되는 건지" 해석이 애매해짐을 지적 — 사용자가 **"도구 자체를 보강 — 어휘 타이핑을 다 마친 후 지문 전문을 보여주는 읽기 전용 단계 추가"**를 선택. 파트 2의 마지막 어휘 단계(읽어보기) 완료 후, 이해도 퀴즈 전에 "읽어보기 지문 전체"(30~31쪽, 6단락)를 이어서 읽는 화면(`screen-passage`)을 새로 구현 — 타이핑 대상이 아니라 순수 읽기 전용(스크롤 가능한 텍스트 박스 + "다 읽었어요, 이해도 확인하기" 버튼). 노출 조건은 퀴즈 게이트(§50)와 동일하게 `quiz_results` 기록 유무로 연동해 파트 2 진입마다 매번 뜨지 않게 함. 지문 원문 6단락 전체를 `week23_content`에 `stage_key='passage'`로 추가(타이핑 검증 루프에서는 제외되도록 `STAGE_DEFS`에는 넣지 않음). 브라우저에서 지문 표시→계속 버튼→퀴즈 연결까지 전체 플로우를 직접 실행해 검증 완료. **다음 단계로 남은 것**: "내용 이해" 문항(예: 중심 사건이 주는 의미는?) 추가 여부는 아직 미정 — 이번엔 도구 보강까지만 하고 문항 추가는 추후 논의하기로 함. RQ1의 2번째 한계(비교 기준 없음)도 아직 미해결(§6)
56. **[내용 이해 문항 구현]** 사용자가 "이 문항들이 연구 주제·연구문제에 부합하냐"고 확인 요청 — 문항 2·3(인과관계 이해)과 특히 문항 4("겨울바람의 의미")가 지문 이해뿐 아니라 파트 2 핵심개념어 "중심 사건이 주는 의미"를 실제 지문에 적용하는 것까지 재고 있어 RQ1과 이 단원 학습목표를 동시에 만족한다고 평가, 문항 1은 상대적으로 얕은 사실 확인이라는 점도 짚음. 사용자 승인 후 구현 — 새 테이블 `week23_comprehension_questions`(part/order_num/question_text/correct_answer/distractor_1~3) 추가, 파트 2용 4문항 반영. 어휘 퀴즈처럼 무작위 샘플링하지 않고 파트에 준비된 문항을 전부 사용. `generateQuiz`(어휘)와 `generateComprehensionQuiz`(내용이해)를 분리해 만들고 `showQuiz`에서 두 배열을 합쳐 하나의 퀴즈로 진행하되, 각 문항에 `type:'vocab'|'comprehension'` 태그를 붙여 `quiz_results.details`에 함께 저장(향후 두 유형을 분리 분석 가능). 어휘 문제에서 내용 이해 문제로 넘어가는 첫 문항에 "📖 이제부터는 방금 읽은 지문의 내용을 이해했는지 묻는 문제예요"라는 구분 안내를 1회만 표시. 내용 이해 문항 로딩은 실패해도(테이블 없음 등) 어휘 퀴즈 자체엔 영향 없게 설계(비필수 기능 원칙 재적용). 디버그 훅으로 12문항(어휘8+내용이해4) 전체 플로우, 9번째 문항에서 구분 안내 노출, 채점·저장 payload의 type 필드까지 실제로 실행해 검증 완료. `supabase_setup_week23.sql`에 테이블·문항 SQL 추가 — **아직 사용자가 실행 전** (실행하면 실제 반영됨, 안 해도 어휘 퀴즈는 정상 작동)
57. **[4~5주차(요약하기) 착수]** 개강(2026-09-02)이 임박해(기록 시점 8/27, 6일 전) 사용자가 "4~5주차부터 바로 시작해야 할 것 같다"고 결정 — 교재 45~64쪽(6단원 "요약을 활용한 글쓰기의 절차와 방법", 7단원 "요약을 활용한 글 작성하기") PDF를 읽고 구조 파악. `week2_3_reflective_writing.html`을 복사해 `week4_5_summary.html` 생성(로그인·타이핑 엔진·IME 처리·퀴즈·지문화면 등 검증된 로직 그대로 재사용), 테이블명 week23→week45 일괄 치환(`quiz_results`는 unit 필드로 구분하는 공유 테이블이라 그대로 유지). STAGE_DEFS를 파트 1(6단원: 핵심개념어+지문어휘)/파트 2(7단원: 핵심개념어) 2파트 구조로 재정의 — "활동 지시문 어휘"(1~8단계 절차 지시, 표현바꿔쓰기 5가지 방법 등)는 성찰적 글과 같은 원칙(수업 중 교수자 설명)으로 처음부터 제외
58. **[요약하기 어휘 큐레이션·검증]** 처음엔 절차 중심으로 파트1 개념어 8개만 초안으로 냈으나, 사용자가 "46~47쪽 자료 지문(기후 위기로 인한 사회적 불평등)이 핵심이고, 이걸 이해해야 공부할 수 있다"고 지적 — 성찰적 글 파트2와 같은 실수(지문 어휘 누락)를 반복할 뻔함. 지문+요약글①②를 문장 단위로 재대조해 지문 어휘 14개 초안→"너무 적다"는 지적에 재검토해 38개로 확장(사용자 원칙: "이 어휘를 모르면 이 글이 이해되나?"). 구현 전 전체 어휘(49개)·지문 3종·내용이해 문항 4개를 검토용 Artifact로 정리해 제공, **SQL/앱에는 아직 아무것도 반영하지 않은 채 어휘만 먼저 확정**하기로 함(사용자: "지금 어휘는 신중하게 해야지! 어휘 확정 후에 프로그램으로 진행시켜"). 검토 중 "핵심 내용"·"인용 표현" 두 항목이 사전적 의미 대신 불필요하게 절차 맥락에 갇힌 뜻풀이였음을 발견해 사전 조합 의미(핵심+내용, 인용+표현)로 수정. 이어서 사용자가 "부유하다=재산이 많다인데 '재산'도 어려운 단어 아니냐"고 지적 — 뜻풀이 안에 또 다른 어려운 단어가 들어가는 "순환 정의" 문제를 같은 기준으로 전체 38개 재검사해 9개(부유하다·불평등·약자·자연재해·추구하다·직간접적으로·여유·뒷받침되다·겪다) 수정. "여유"의 영어·중국어 번역은 "여유가 있다/없다"라는 실제 용례로 판단해 "room to spare"/"余地"로 확정(从容은 다른 의미(태도)라 제외) — 실제 지문 문장("편리함을 누릴 여유가 없는 사람들")으로 재확인해 뒷받침
59. **[퀴즈 다국어 개선 — 한국어 뜻풀이 편향 문제 발견]** 사용자가 "타이핑할 땐 영어·중국어 뜻도 같이 보는데, 퀴즈는 한국어로만 물어보는 게 맞나?"라고 질문 — 처음엔 "실제 수업은 한국어로만 진행되니 번역 없이 테스트하는 게 더 타당하다"고 답했으나, 사용자가 "영어·중국어로 뜻을 알아도 한국어 수업을 이해 못 하는 게 아니라 오히려 더 확실히 이해한 상태로 듣는 거 아니냐"고 재반박 — 이 지적이 맞음을 인정. 1차로 `generateQuiz`에서 뜻풀이 표시 언어를 한국어/영어/중국어 중 **무작위로 하나만** 고르도록 구현했으나(`pickMeaning` 헬퍼), 실제 화면에서 확인한 사용자가 "퀴즈가 중국어로만 나온다, 한글·영어·중국어 다 나와야지"라고 피드백 — 애초 취지("언어와 무관하게 어휘 지식 자체를 재기")를 살리려면 하나만 무작위로 보여주는 게 아니라 **세 언어를 동시에** 보여주는 게 맞다고 판단해 재설계함. `meaningHtml`/`meaningPlain` 헬퍼로 교체, 퀴즈 옵션을 `{html, plain, isCorrect}` 구조로 바꿔 화면엔 한국어+EN 태그+영어+中 태그+중국어를 타이핑 화면(gloss-box)과 동일한 스타일로 함께 표시(`.quiz-meaning`/`.m-tag` CSS 추가). `week2_3_reflective_writing.html`·`week4_5_summary.html` 둘 다 동일 적용, 브라우저에서 실제 데이터로 word→meaning/meaning→word 두 방향 모두 세 언어가 함께 나오는지, 채점·`quiz_results` 저장(다국어 텍스트 그대로 plain 필드에 기록)까지 실행해 검증 완료. 사용자가 "더 타이핑하고 싶으면 어떻게 하냐"고 물어 확인한 결과 기존 "더 연습하기" 버튼(체크포인트 화면)이 이미 그 역할을 하고 있음을 재확인(신규 구현 불필요)
60. **[퀴즈 중간 이탈 시 복귀 경로 없음 — 1차 수정, §61에서 최종 설계로 대체됨]** 사용자가 요약하기 파트 1 퀴즈를 풀다가 파트 선택으로 나갔는데, 다시 퀴즈로 돌아갈 방법이 없어서 38개 지문 어휘를 처음부터 다시 타이핑해야 하는 상황을 실사용 중 발견·보고. 원인: 퀴즈는 마지막 어휘 단계를 "완료 처리하는 순간"에만 자동으로 뜨고, `quiz_results`엔 퀴즈를 끝까지 마쳐야만 기록이 남아서, 중간에 나가면 재진입 지점이 전혀 없었음. 1차 수정으로 `showPartSelect()`를 async로 바꾸고, 파트가 다 끝났는데(`allDone`) 아직 `quiz_results` 기록이 없는 경우(`hasQuizResult` 재사용) 그 파트 카드에 "이해도 확인하러 가기 →" 버튼을 추가로 노출하는 방식을 구현·검증까지 완료했으나, 이후 사용자가 §61의 더 나은 설계를 제안해 이 버튼 방식은 완전히 제거되고 대체됨(아래 참고)
61. **[퀴즈 접근 방식 재설계 — 로드맵 칩으로 이동, 언제든 무제한 재응시]** §60 수정 직후 사용자가 "성찰적 글 파트2·3은 왜 이해도 확인하러 가기가 없냐"고 재질문 — 확인 결과 그 파트들은 이미 실제로 퀴즈를 봐서(`quiz_results` 기록 존재) 버튼이 안 뜨는 정상 동작이었음(재노출 방지 원칙대로). 이 대화 중 사용자가 근본적으로 더 나은 설계를 제안: "여기 옆에 두는 건 어때? 앞에 걸 하고 뒤에 걸 푼다. 그리고 언제든지 다시 풀 수 있다" — 퀴즈 진입점을 파트 선택 화면의 일회성 버튼이 아니라, **연습 화면 로드맵 안에 어휘 단계 칩들과 나란히 놓인 상시 칩**(📝 이해도 확인)으로 옮기고, 그 파트 어휘 단계를 모두 마치면 잠금 해제되어 그 뒤로는 이미 봤든 안 봤든 상관없이 **언제든 다시 클릭해 재응시 가능**하게 바꿈(재응시할 때마다 `quiz_results`에 새 기록이 쌓임 — 단계 연습 "N회" 기록과 동일한 방식). §60의 `hasQuizResult` 기반 1회성 게이트 자체를 완전히 대체 — `showPartSelect()`는 다시 동기 함수로 되돌리고 "이해도 확인하러 가기" 버튼은 제거, 대신 `renderRoadmap()`이 어휘 단계 칩들 뒤에 `.quiz-chip`을 추가로 렌더링(모든 어휘 단계 완료 시 `clickable`, 아니면 `locked`), 클릭 시 `showQuiz(part, ...)`를 직접 호출. `week2_3_reflective_writing.html`에서 구현·디버그 훅으로 검증(잠금 상태·해제 후 클릭·재응시 시 새 기록 저장까지 확인) 후 `week4_5_summary.html`에도 동일 이식 — 이식 결과는 코드 정적 검토(week23 잔재 없음, `STAGE.part`/`STAGE_DEFS` 기반 잠금 조건 일치, `showQuiz(STAGE.part, ...)` 호출부 일치)로 확인
62. **[분반/이름/학번 재입력 부담 해소 — 로그인 정보 브라우저 저장]** 사용자가 "분반·이름·학번, 이거 한 번 입력하면 웹페이지 새로 여는 거 아닌 이상 그대로 두면 안 되려나? 앞으로 갔다 올 때마다 계속 반복해야 하는 거 너무 지쳐"라고 피드백 — 기존 §47의 "이어서 진행하시겠어요?" 복원은 같은 세션(페이지를 새로고침하지 않는 동안) 메모리에만 남아 있어서, 실제로 페이지를 새로 열면(브라우저 종료 후 재접속 등) 다시 무력화되는 한계가 있었음. `localStorage`에 참여자 코드·이름을 저장하는 `saveIdentity`/`clearIdentity`/`loadSavedIdentity` 함수를 추가 — 키(`hangul_typing_identity`)를 프로그램별로 나누지 않고 공유해서, 같은 학생이 1주차·2~3주차·4~5주차를 오갈 때도 동일 인물이니 매번 다시 확인할 필요가 없게 함(배포 시 같은 도메인이라 공유됨). 로그인 성공 시(`enterProgram`) 저장, "다른 학생으로 로그인" 클릭 시 삭제, "내 기록 보기" 진입 시에도 유지되도록 저장. 페이지 로드 시 저장된 값이 있으면 `currentCode`/`currentName`을 미리 채워 시작 화면에 "이어서 진행하시겠어요?" 박스를 즉시 보여줌 — "계속하기" 버튼은 `enterProgram()`을 다시 호출해 Supabase에서 진행 상태를 새로 불러오게 함(메모리 상태는 새로고침 시 사라지므로). `week2_3_reflective_writing.html`·`week4_5_summary.html` 둘 다 동일 적용. **검증 관련 특이사항**: 브라우저 자동화 도구(로컬 `file://` 페이지를 내부적으로 `data:` URL로 감싸 렌더링)에서는 `Storage is disabled inside 'data:' URLs` 오류가 나서 `localStorage` 자체를 이 도구로는 직접 실행 검증할 수 없었음 — 이는 도구 자체의 샌드박스 제약이지 코드 결함이 아님(§59에서 이미 검증된 로직·try/catch 방어 패턴을 그대로 재사용). 코드 정적 검토로 로직 정합성은 확인했으나, 실제 브라우저에서의 최종 동작 확인은 사용자가 직접 함 — **사용자가 실제로 새로고침 후 "이어서 진행하시겠어요?" 박스가 뜨는 것을 확인**(§64로 이어짐)

63. **[퀴즈 화면에 이전 문제 되돌아보기 기능 추가]** 사용자가 퀴즈를 풀던 중 스크린샷과 함께 "여기에 뒤로 버튼 넣어서 뒷문제 볼 수 있게 하는 건 어때? 다시 확인하고 싶어서"라고 요청 — 지금까지는 한 번 답을 고르면 바로 채점·잠기고 자동으로 다음 문제로 넘어가기만 할 뿐, 이전 문제로 돌아갈 방법이 전혀 없었음. "← 이전 문제"/"다음 문제 →" 버튼을 옵션 아래에 추가하되, **연구 데이터 신뢰성을 위해 되돌아간 문제는 조회만 가능하고 이미 낸 답은 바꿀 수 없게** 설계(리뷰 모드 — 정답에 `correct`, 실제 골랐던 오답에 `wrong` 클래스를 그대로 재현, 옵션 버튼은 비활성화, "✓ 이미 답변한 문제예요" 안내 문구 표시). `quizIdx`(지금 보는 위치)와 `quizAnswers.length`(실제 답한 문제 수)를 분리해 판단 — 다시 앞으로 가면 이미 답한 구간은 리뷰 모드로, 아직 안 푼 최신 문제에 도달하면 원래처럼 클릭 가능한 활성 모드로 자동 전환됨. 진행 바(%)는 보는 위치가 아니라 실제 완료된 문제 수 기준으로 계산해, 뒤로 가도 막대가 줄어들지 않게 함. `week2_3_reflective_writing.html`에서 디버그 훅으로 정답 선택→다음 문제→이전 클릭(정답 리뷰 확인)→다시 다음→오답 선택→두 칸 뒤로(오답 리뷰에서 `wrong`/`correct` 클래스 정확히 재현되는지)까지 전체 시나리오를 직접 실행해 검증 완료 후 훅 제거, `week4_5_summary.html`에도 동일 이식(코드 정적 검토로 일치 확인)
64. **[로그인 화면: 저장된 로그인이 있으면 폼 자체를 감춤]** §62 배포 후 사용자가 실제 화면 스크린샷으로 "이게 학생들이 계속 이어서하기 안 보여. 바로 분반 다시 선택할 것 같아. 이어 진행하기만 있어도 되지 않아?"라고 지적 — "이어서 진행하시겠어요?" 박스는 정상적으로 뜨고 있었지만(§62 localStorage 복원이 실제로 작동함이 이 스크린샷으로 처음 확인됨), 그 바로 아래에 분반·이름·학번 입력 폼 전체가 그대로 남아 보여서, 학생이 위 박스를 무시하고 그냥 폼부터 채우기 쉬운 레이아웃이었음. 분반/이름/학번/시작 버튼/명단에 없어요 등 로그인 폼 전체를 `loginFormBox`로 묶고, 저장된 로그인이 있을 땐 이 박스를 통째로 감춰 화면에 "이어서 진행하시겠어요?" 박스만 보이게 함 — 폼은 "다른 학생으로 로그인"을 눌러야 다시 나타남(같은 기기를 다른 학생이 쓰는 경우 대비). `week2_3_reflective_writing.html`·`week4_5_summary.html` 둘 다 동일 적용
65. **[퀴즈 결과 화면에 문제별 정답 확인 목록 추가]** 사용자가 "여기서 문제 확인할 수 있어? 저기서 보려니까 내가 문제를 계속 풀어야 해서"라고 물은 뒤, 실제로 퀴즈를 다 풀고 결과 화면(8문제 중 3개 정답)에 도달한 스크린샷을 보내며 원인을 스스로 짚어냄 — "이게 내가 답을 누르면 바로 답인지 오답인지가 나오고 다음 문제로 넘어가는거야. 그래서 맞는 답을 확인하기가 어려워", 이어서 "계속하기 누르면 또 처음으로 돌아가, 선택지가 그거밖에 없어서"라고 막힌 지점을 정확히 설명. 즉 문제 하나당 정답 표시 후 0.7초 만에 자동으로 다음 문제로 넘어가 버려 차분히 확인할 시간이 없었고, 다 풀고 난 결과 화면에는 총점(%)만 있을 뿐 문제별 정답·오답 내역이 전혀 없어서 "계속하기"로 나가는 것 외엔 선택지가 없었음. 결과 화면(`quizResultBox`)에 총점 아래로 문제별 목록(`quizReviewList`)을 추가 — 이미 `quizAnswers`에 기록돼 있던 값(문항·내 답·정답·정오 여부)을 그대로 재사용해 렌더링만 새로 함(추가 데이터·API 호출 불필요). 맞은 문제는 teal, 틀린 문제는 brick(붉은) 배경으로 구분하고 틀린 문제엔 정답을 함께 표시 — 시간 제한 없이 스크롤하며 볼 수 있음. `week2_3_reflective_writing.html`에서 디버그 훅(실제 DB에 쓰지 않도록 렌더링 로직만 분리 실행)으로 스크린샷까지 찍어 레이아웃·색상 검증 완료 후 훅 제거, `week4_5_summary.html`에도 동일 이식. (§63의 "이전 문제" 되돌아보기는 퀴즈를 푸는 도중 확인용으로 계속 남겨둠 — 둘은 겹치지 않고 서로 보완: §63은 풀면서, 이번 §65는 다 풀고 난 뒤 한눈에)
66. **[퀴즈 자동 진행(0.7초 후 다음 문제) 폐지 → 수동 "다음 문제" 버튼으로 전환]** §65에서 사용자가 짚은 근본 원인("답을 누르면 바로 정오답이 나오고 자동으로 다음 문제로 넘어가서 확인하기 어려움")을 사용자가 직접 "다음 문제 버튼을 만들까?"로 제안 — 결과 화면 목록(§65)은 다 풀고 난 뒤의 사후 확인이었다면, 이번엔 문제를 푸는 그 순간 자체의 시간 압박을 없앰. `answerQuiz`에서 `setTimeout(...,700)` 자동 진행 로직을 제거하고, 답을 고르면 그 자리에서 `loadQuizQuestion()`을 다시 불러 **같은 문제를 리뷰 모드로 즉시 재표시**(정답·오답 색이 시간 제한 없이 유지됨) — §63에서 이미 만들어 둔 "다음 문제 →" 버튼이 정확히 이 순간에도 자연스럽게 나타나므로 새 버튼을 따로 만들 필요 없이 재사용됨(`quizIdx < quizAnswers.length`이면 리뷰 모드라는 §63의 판단 기준이, 답을 막 제출한 직후에도 그대로 성립하기 때문). "다음 문제" 클릭 시 마지막 문제였으면 결과 화면(`finishQuiz`)으로, 아니면 다음 문제로 넘어가도록 `goToNextQuiz()` 함수로 정리(§63의 리뷰 되돌아가기용 다음 버튼과 로직 통합). `week2_3_reflective_writing.html`에서 디버그 훅으로 답 선택 시 자동 진행이 없는지, 다음 버튼 클릭 시 정확히 다음 문제로/마지막엔 결과 화면으로 넘어가는지, 실제 DB에 쓰지 않고 전체 시나리오를 스크린샷까지 찍어 검증 완료 후 훅 제거, `week4_5_summary.html`에도 동일 이식
67. **[정답/오답 판정을 더 확실하게 보이도록 개선]** §66 직후 사용자가 "퀴즈 맞았다 틀렸다가 확실히 보이는 게 좋을 것 같아"라고 요청 — 기존엔 옵션 버튼의 테두리·배경 색(teal/brick)만으로 정오답을 구분해서, 다국어 뜻풀이 텍스트가 많은 옵션 안에서 색 차이가 눈에 덜 띌 수 있었음. 문제 바로 위(기존 `quizReviewNote` 자리)에 **크고 색이 뚜렷한 판정 배너**를 추가 — 정답이면 teal 배경에 "✓ 정답이에요!", 오답이면 brick 배경에 "✗ 오답이에요 — 정답: OO"를 굵고 큰 글씨(16px)로 표시하고, 그 아래 작은 글씨로 "확인만 가능하고 답은 바뀌지 않아요" 안내를 유지. 옵션 버튼에도 정답엔 ✓, 골랐던 오답엔 ✗ 아이콘을 앞에 추가해 색맹 등 접근성까지 이중으로 보강. `week2_3_reflective_writing.html`에서 디버그 훅으로 정답·오답 두 경우 모두 스크린샷을 찍어 배너·아이콘이 의도대로 보이는지 확인 후 훅 제거, `week4_5_summary.html`에도 동일 이식
68. **[요약하기 내용 이해 문항 3번 — 지문 내용만으로 풀 수 없는 문제 발견·교체]** 사용자가 실제 퀴즈 11/12번 문제 스크린샷과 함께 "지문 내용만 확인하는 문제면 돼. 이건 학생들이 배우지 않은 상태에서 풀기 어려운 문제야"라고 지적. 문제였던 3번 문항("'요약을 활용한 글 ②'는 원문(자료)에 없는 내용을 추가로 담고 있다. 그것은 무엇인가?")은 원문·요약①·요약②**세 텍스트를 비교해서 어느 부분이 새로 추가된 의견인지 분석**해야 풀리는 문제였음 — 이건 정확히 6단원(요약을 활용한 글쓰기의 절차와 방법) 수업이 가르치는 분석 기법(표현 바꿔 쓰기·인용 표현·자신의 의견 덧붙이기의 실제 적용) 그 자체라, 사전학습(수업 전) 단계에서 풀기엔 아직 안 배운 기술을 요구하는 문제였음. 나머지 1·2·4번은 원문 한 단락만 읽으면 바로 답할 수 있는 사실 확인형이라 문제없다고 판단, 3번만 같은 기준(지문 내용만으로 답변 가능한가)으로 교체 — 새 문항은 4번째 단락(홍수 사례)에서 "가난한 사람들이 부유한 사람들보다 홍수 피해를 더 크게 입는 이유"를 그대로 묻는 사실 확인형으로 바꿈. `supabase_setup_week45.sql`의 해당 INSERT 행을 새 문항으로 교체, 실제 DB 반영용 UPDATE문을 별도로 제공(사용자가 Supabase에서 직접 실행 예정) — **이 대화 시점엔 아직 DB에 반영 전**
69. **[내용 이해 문항 난이도 기준 일반화 — "지문만 읽으면 풀리는 문제"로 재확인]** §68 직후 사용자가 "퀴즈가 너무 어려우면 안 돼. 글 읽고 많이 생각하지 않아도 풀 수 있는 문제면 좋아"라고 원칙을 명시 — 이 기준으로 남은 문항 전체(요약하기 1·2·4번 + 성찰적 글 4문항)를 다시 검토. 요약하기 나머지 3문항은 답이 한 단락 안에 거의 그대로 있어 문제없음. 성찰적 글은 1·2·3번은 문제없으나, **4번("겨울바람은 결국 어떤 의미가 되었나?")**이 질문 표현 자체가 상징적 해석을 요구하는 것처럼 보여 애매하다고 판단해 확인 요청 — 정답 문구는 마지막 단락에 거의 그대로 있어("그 날이... 원동력이 되었다") 실제로는 못 찾을 정도는 아니지만, "의미"라는 질문 방식이 부담을 줄 수 있어 더 사실 확인형 문구("그날의 경험은 결국 무엇이 되었나?")로 바꾸는 안을 제시함 — **사용자가 "그대로 두기"를 선택**해 문항은 원문 그대로 유지, `supabase_setup_week23.sql`·DB 변경 없음
70. **[퀴즈 재응시 후 "계속하기" 도착 화면 수정]** 사용자가 요약하기 퀴즈(로드맵 칩으로 재응시)를 마치고 "계속하기"를 눌렀더니 방금까지 보고 있던 단계(이미 완료한 "핵심 개념어" 1/8, "요약" 타이핑 화면)가 그대로 다시 나오는 걸 스크린샷으로 보여주며 "이게 맞아?"라고 질문. 원인 확인: §61에서 로드맵 퀴즈 칩의 onDone을 `renderRoadmap(); showScreen('practice');`로 만들었는데, `STAGE`(현재 보던 단계) 값이 그대로 남아 있어서 퀴즈 진입 직전에 보던 단계 화면이 재사용되는 것이었음(버그는 아니고 진행 상태·기록도 전혀 안 건드림 — 다만 "이해도 확인 다 봤는데 왜 갑자기 다시 타이핑 화면?"이라는 인상을 줄 수 있음). 처음 자동으로 뜨는 퀴즈(`showQuizIfNeeded`)는 원래부터 퀴즈 후 파트 선택 화면(`showPartSelect`)으로 보내도록 돼 있었는데, 로드맵 칩을 통한 재응시만 다르게 동작하던 것 — 사용자에게 "파트 선택 화면으로/지금처럼 단계 화면 그대로" 중 선택지를 물어 **파트 선택 화면으로** 결정. 로드맵 칩의 onDone을 `showPartSelect`로 통일(자동 트리거 퀴즈와 동일한 목적지). `week2_3_reflective_writing.html`·`week4_5_summary.html` 둘 다 동일 적용 — `showPartSelect()`가 순수하게 메모리 상태(completedStages 등)만으로 동기 렌더링되는 함수라 별도 브라우저 검증 없이도 안전하다고 판단(이미 검증된 자동-트리거 경로와 완전히 동일한 호출 패턴 재사용)
71. **["처음으로" 버튼이 프로그램 내부 로그인 화면이 아니라 전체 랜딩 페이지로 가도록 수정]** 사용자가 `index.html`(전체 프로그램 목록) 스크린샷과 함께 "처음으로 버튼 누르면 이 화면이 나와야할 것 같아"라고 지적 — 그동안 파트 선택 화면과 기록(내 기록 보기) 화면의 "← 처음으로"/"처음으로" 버튼이 실제로는 `index.html`이 아니라 **그 프로그램 자신의 로그인 화면**(`showStartScreen()`/`showScreen('start')`)으로만 돌아가고 있었음 — 라벨("처음으로")과 실제 동작(그 프로그램 안에 계속 머무름)이 어긋나 있던 것. "다른 학생으로 로그인"이 이미 학생 전환 용도로 따로 있어서, "처음으로"는 이름 그대로 전체 랜딩 페이지로 보내도 기능이 겹치지 않는다고 판단 — `window.location.href = 'index.html'`로 교체. `week2_3_reflective_writing.html`·`week4_5_summary.html`뿐 아니라 같은 패턴이 있던 `stage0_jari_practice.html`(1주차)에도 동일하게 적용해 세 프로그램 전부 일관되게 맞춤(연습 화면의 "← 파트 선택으로" 버튼은 라벨이 다르고 실제로도 그 프로그램 안에서 파트 선택으로 돌아가는 게 맞아서 그대로 둠)
72. **[지문 문단 첫 줄 들여쓰기 추가]** 사용자가 "지문 문단은 잘 나뉘었는데 문단 첫 문장 들여쓰기가 안 되어 있다 — 이건 수업 시간에도 가르치는 중요한 부분이라 꼭 지켜야 한다"고 지적. `.passage-text p`에 `text-indent:1em`을 추가 — 15.5px 폰트 기준 15.5px(글자 한 칸 폭)만큼 첫 줄이 들여써짐. `week2_3_reflective_writing.html`(성찰적 글 지문)·`week4_5_summary.html`(원문/요약①/②, `.passage-section` 안에 중첩된 `<p>`도 같은 `.passage-text p` 선택자에 걸려 자동 적용됨) 둘 다 적용, `getComputedStyle`로 실제 적용값(15.5px) 확인해 검증 완료
73. **[research_export.html 정비 + 지문 읽기 재접근 기능 추가]** 대화 중 "지문은 프로그램 어디에 있냐"는 질문에 답하다가 두 가지 허점을 발견. (1) `research_export.html`을 열어보니 `quiz_results` 다운로드 버튼은 §6에 "추가 필요"로 남아있던 것과 달리 이미 구현돼 있었지만(과거 세션에서 반영되고 §6이 갱신 안 된 것으로 추정) 설명 문구가 "파트를 처음 완료할 때마다 한 행"으로 §61 이전 상태를 가리키고 있었고(재응시마다 쌓이는 현재 동작과 불일치), **4~5주차(요약하기) 섹션 자체가 통째로 빠져 있었음**(records/progress 다운로드 불가) — 설명 문구를 현재 동작에 맞게 고치고 4~5주차 카드(`week45_records`/`week45_progress`)를 새로 추가. (2) 지문 읽기 화면(`screen-passage`)이 어휘를 처음 다 마쳤을 때 딱 한 번만 자동으로 뜨고 다시 볼 방법이 없다는 것을 사용자가 인지 — "지문도 읽고 싶을 때 바로 읽어볼 수 있어야 한다"고 요청. §61의 이해도 확인 칩과 동일한 패턴(어휘 단계 모두 마치면 잠금 해제, 그 뒤론 언제든 클릭)으로 로드맵에 **"📖 지문 읽기" 칩**을 추가(퀴즈 칩 바로 앞) — 지문 콘텐츠가 없는 파트(성찰적 글 파트 1·3, 요약하기 파트 2)에서는 `contentByStage`에 해당 `part:passage` 키가 비어 있으므로 칩 자체가 안 뜸. 처음엔 칩으로 들어갈 때만 "다 읽었어요, 돌아가기"(로드맵 복귀)로 다르게 표시했으나, 사용자가 실제 화면을 보고 "돌아가기가 맞나? 문제 풀러 가야 하잖아"라고 바로 지적 — 지문을 다시 읽는 것도 결국 이해도 확인으로 이어지는 게 자연스럽다고 판단해 **칩으로 들어가도 항상 퀴즈로 연결**되도록 재수정(`continueLabel` 매개변수 자체를 없애고 단일 동작으로 통일). `week2_3_reflective_writing.html`·`week4_5_summary.html` 둘 다 적용
74. **[요약하기 지문 읽기 화면에서 요약①② 제외 — 원문만 남김]** 사용자가 "요약을 활용한 글1,2는 뺄까 봐. 위에 지문 내용만 정확하게 알면 되거든"이라고 제안. 검토 중 지문 어휘 38개 중 6개(지적하다·뒷받침되다·배려하다·완화·실천·의지)가 원문 6단락엔 없고 요약②에만 실제로 쓰인다는 걸 발견해 먼저 확인받음 — 다만 요약②는 교재 46~47쪽에 그대로 있어 수업 시간에 다뤄지므로 이 어휘들을 계속 가르치는 근거는 유효하다고 판단, **어휘는 그대로 두고 지문 화면에서만 요약①②를 제외**하기로 확정("연구에 지장 없지?"라는 질문에 — RQ1 측정용 이해도 확인 퀴즈 4문항이 모두 원문 기반이라 요약①②를 화면에서 빼도 측정 도구엔 영향 없음, 오히려 "원문 내용 이해까지가 이 도구의 역할, 요약 기법 자체는 수업 시간 몫"이라는 기존 원칙에 더 맞음을 확인). `supabase_setup_week45.sql`에서 요약①(order_num 7)·요약②(8·9) 행 삭제, 원문 6개만 남김. `showPassage()`를 섹션 구분 로직 없이 단락만 이어 붙이는 단순한 형태로 되돌리고(§73에서 추가했던 섹션 헤더 로직 제거), `.passage-section`/`.passage-section-title` CSS도 정리. 화면 배지·안내 문구("자료(원문)와 요약 예시" → "자료(원문)", "원문이 어떻게 요약으로 바뀌는지 비교" 삭제)도 함께 수정
75. **[프로그램 안내사항 3개 추가 + 랜딩 페이지·인덱스 카드 정비]** 한 번에 여러 작은 요청 처리. (1) 사용자가 "타이핑 점수·이해도 점수는 성적 반영 안 됨, 컴퓨터로 진행해야 함(타이핑 실력 향상 목적), 수업 전에 미리 해야 함"을 안내에 넣어달라고 요청 — 세 프로그램(`stage0_jari_practice.html`·`week2_3_reflective_writing.html`·`week4_5_summary.html`) 모두의 시작 화면 "📋 이렇게 진행됩니다" 안내에 4~6번 항목으로 추가(1주차는 이해도 퀴즈가 없어 6번 문구만 "정확도나 속도는 성적에 반영되지 않아요"로 다르게). (2) `index.html` 카드 스크린샷을 보고 "1주차, 2~3주차 < 이 말이 필요할까?"라고 질문 — 배지("2~3주차")와 설명 문구 첫머리("주2~3 대응")가 같은 정보를 중복 표시하고 있던 것을 발견, 설명에서 "주N~N 대응" 부분만 제거(1주차 카드는 애초에 이런 중복이 없어서 그대로 둠)
76. **[요약하기 파트 2·3 구조 확정 + 파트 2 어휘 착수]** 사용자가 "오늘 목표는 요약 파트2, 파트3 완성하는 거야"라고 선언 — 기존에 2파트(6단원/7단원)로만 알고 있던 구조가 실제로는 **파트 1(45~54쪽, 6단원, 완성됨)/파트 2(55~61쪽)/파트 3(62~64쪽)**의 3파트임을 확인(7단원 55~64쪽을 사용자가 페이지 기준으로 다시 나눈 것 — 7단원 자체가 "1.요약하기(55~61, 혐오 표현 예제로 8단계 절차 실습)"와 "2.요약을 활용한 글 작성하기(62~64, AI 지문으로 최종 작성 + 64쪽 문어체 TIP)"로 나뉘는 교재 구조와도 자연스럽게 대응). 파트 2·3 핵심 개념어는 파트 1과 8단계 절차가 완전히 동일해 새로 필요 없다고 판단, 진짜 새로 필요한 건 각 파트의 예제 지문(파트2: 혐오 표현 자료, 김영희·시민일보 / 파트3: AI 지문, 박민수·미래사회) 어휘임을 확인. 파트 3의 64쪽 "문어체" 코너(구어체·문어체 대응표, 문장 종결·부정문·주어 생략 규칙)는 단어 하나하나의 뜻이 아니라 **표현 변환 규칙**이라 기존 "어휘 타이핑" 방식과 성격이 달라 어떻게 다룰지는 보류 — 사용자가 "파트2 어휘부터 뽑자"고 해서 파트 3 처리 방식 논의는 다음으로 미룸. 파트 2(혐오 표현 지문, 5단락)를 문장 단위로 대조해 지문 어휘 초안 39개를 뽑아 제시했으나, 사용자가 "이 단어만 알아도 다 이해할 수 있어?"라는 §39 원칙으로 재검증을 요구 — 다시 전수 대조해 7개 누락 발견(차별·성적 지향·표현의 자유·원칙·집단 괴롭힘·도덕적·기준), 총 46개로 확정. 한국어 뜻풀이 작성 후 사용자가 "꺼리다"(혐오 정의에 사용)·"맞서다"(반감 정의에 사용)가 초중급 학습자에게 너무 어렵다고 지적한 것을 계기로 전체를 순환 정의 기준으로 재검사해 9개 추가 수정(편견·차별·고립되다·단호하다·굳어지다·집단 괴롭힘·무너뜨리다·용인되다·기준 — 특히 "집단 괴롭힘"과 "무너뜨리다"는 정의 안에 자기 자신의 어근(괴롭히다/무너지다)을 쓰는 진짜 순환 정의였음). 사용자가 영어·중국어를 직접 검증할 수 없다며 "한국어 뜻풀이에 맞는 영어·중국어인지 확인해 달라"고 요청 — 46개 전수 대조해 5개 불일치 발견: 맥락(中 语境은 언어적 맥락만 가리켜 "사회적 맥락"에 안 맞음 → 背景로), 사회적 약자(En "social minority"는 "수가 적음"을 뜻해 "힘없음"과 다름 → "socially vulnerable group"으로), 통합(한국어 풀이가 너무 일반적이라 中 团结의 "사회적 결속" 의미와 어긋남 → 풀이를 "여러 사람이나 집단이 서로 잘 어울려 하나가 되는 것"으로 구체화), 무너뜨리다(中 摧毁는 "완전 파괴"라는 강한 뜻이라 원래 뉘앙스보다 셈 → 破坏로), 실질적("도움이 됨"에 초점 맞춘 한국어 풀이가 실제 뜻(겉치레가 아니라 실제로 이루어짐)과 어긋남 → 풀이 수정). "침해하다"·"위협하다"·"맥락"은 사전적 원뜻과 실제 지문 속 쓰임이 다르거나 두 뜻이 다 있는 경우라 "바람"(§53) 때와 같은 방식으로 두 뜻을 함께 표기(위협하다: ①협박하다 ②위험에 빠뜨리다, 여기서는 ② / 맥락: ①문맥 ②상황적 배경, 여기서는 ②). **46개 전체 확정, SQL·앱 반영은 아직 시작 전** — 다음은 파트 3(AI 지문 어휘 + 64쪽 문어체 코너 처리 방식) 착수 예정
77. **[요약하기 파트 2 SQL·앱 구현 완료]** §76에서 확정한 46개 어휘 + "혐오 표현" 지문 5단락 + 출처 표기를 `supabase_setup_week45.sql`에 반영(`week45_content`에 `part=2` 행 추가, 지문 어휘 46개 + 지문 5단락 + 출처 1행). 앱 쪽에서는 `STAGE_DEFS`에 파트 2 "지문 어휘" 단계 추가, `PART_DEFS`를 3파트 구조로 갱신(파트 2 `ready:true`로 전환, 이름을 교재 소제목 그대로 "7단원 · 1. 요약하기"/파트 3 "7단원 · 2. 요약을 활용한 글 작성하기"로 명명 — §44 원칙과 동일). 지문 읽기·퀴즈 자동 노출 로직(`showQuizIfNeeded`, `showPassage`)이 파트 1로만 하드코딩돼 있던 것을 파트 번호 매개변수 기반으로 일반화해 파트 2에도 그대로 적용되게 함. 출처 표기("김영희(2025.7.5.)...")는 `gloss='출처'`인 행으로 저장하고, `showPassage()`에서 이 값이면 `.passage-credit`(오른쪽 정렬, 옅은 색) 스타일로 다르게 렌더링 — 이 CSS는 원래 성찰적 글에만 있던 걸 요약하기에도 새로 추가함. `index.html` 카드 설명도 "파트 1·2 필수 어휘 제공, 파트 3은 준비 중"으로 갱신. 디버그 훅으로 파트 선택 화면(파트 1 완료·파트 2 진행 중·파트 3 준비 중 배지 정상 표시), 로드맵(파트 2 지문 어휘·지문 읽기·이해도 확인 칩 모두 정상 노출), 지문 화면(출처 행만 `.passage-credit`로 구분 렌더링)까지 실행해 검증 완료 후 훅 제거. 사용자가 실제로 SQL을 실행 — REST로 재조회해 `week45_content`에 파트 2 지문 어휘 46행 + 지문 6행(5단락+출처)이 정확히 반영됐음을 확인. 이어서 파트 2 내용 이해 문항 4개(2·3·4·5단락에서 하나씩, 지문만 읽으면 풀리는 사실 확인형 — §69 원칙)를 제시해 승인받고 같은 SQL 파일의 `week45_comprehension_questions` insert에 파트 1·2 문항을 함께 반영(재실행 필요, 사용자에게 안내함). 사용자가 SQL을 재실행했고 REST로 재조회해 파트 1·2 문항 8개(각 4개씩) 모두 정확히 반영됨을 확인 — **파트 2 완전히 완료(지문 어휘 46개+지문 5단락+이해도 확인 4문항, 전부 실제 DB 반영·확인 완료)**
78. **[파트 2/3 이름 충돌 해결 + 문어체 코너 제외 확정 + 파트 3 어휘 착수]** 사용자가 요청한 대로 파트 2 이름을 교재 단원 제목 그대로 "7. 요약을 활용한 글 작성하기"로 바꾸자, 원래 파트 3에 붙여뒀던 임시 이름("7단원 · 2. 요약을 활용한 글 작성하기")과 거의 같아져 구분이 안 되는 문제 발견 — "같이 논의해 보자"는 사용자 요청으로 세 가지 안(①②suffix / 내용으로 구분 / 페이지로 구분)을 제시했고, 사용자가 **페이지로 구분**을 선택. 이후 사용자가 형식을 더 구체적으로 지정 — "요약을 활용한 글 작성하기1"(파트2, 55~61쪽)/"...2"(파트3, 62~64쪽)로 확정, 별도의 `pages` 필드로 쪽수를 아래 줄에 표시(기존 구조 그대로 활용). §76에서 보류했던 **64쪽 "문어체" 코너는 사용자가 "빼도 될 것 같다"고 최종 결정 — 이 프로그램에서 아예 다루지 않기로 함**(표현 변환 규칙이라 어휘 타이핑 방식과 안 맞고, 이것도 실제 수업 시간에 다룰 수 있는 내용이라 판단). 파트 3 지문("앞서가는 AI, 우리에게는 시간이 필요하다", 박민수·미래사회 5월호, 5단락)을 PDF 원문으로 재확인(1차 읽기와 일치 확인 — "재교육 기회가 적은 계층...접근하는 데에 제약이 없다" 문장이 논리적으로 이상해 보이지만 원문에 그대로 인쇄되어 있어 그대로 채택, 사용자에게 실물 교재 대조를 권함)하고 문장 단위로 대조해 지문 어휘 47개 초안 제시 — 사용자가 다시 "이 단어들만 가지고 이해할 수 있는지" 재검증을 요구했으나 이번엔 재검토 결과 누락 없음 확인(파트1·2에서 이미 배운 확산되다·위협하다·대응하다·차별적·불평등은 단순 재등장이라 의도적으로 제외했음을 설명). 한국어 뜻풀이 작성 시 이번엔 순환 정의를 미리 점검해서 제출(치우치다 같은 이전 실수 재발 방지). 사용자가 "학습하다"(AI가 데이터로 훈련되는 특수한 뜻) 정의에서 "(AI가)"라는 명시적 표현을 빼 달라고 요청 — 주어 없이도 지문 맥락상 AI임이 드러난다고 판단해 "많은 자료를 보고 스스로 규칙이나 방법을 찾아 익히다"로 수정. 영어·중국어 대조 검증에서 中 번역에 "(AI)"가 그대로 남아있던 것을 발견해 함께 수정(通过大量数据自主掌握规律或方法). 47개 전체 확정 후 SQL(`week45_content`에 `part=3` 지문 어휘 47개+지문 5단락+출처)·앱(`STAGE_DEFS`에 파트 3 단계 추가, `PART_DEFS` 파트 3 `ready:true` 전환) 모두 구현, 디버그 훅으로 파트 선택·로드맵·지문 화면 전부 검증 완료 후 훅 제거. 내용 이해 문항 4개(2·3·4·5단락 기준 사실 확인형)도 제시해 승인받아 SQL에 반영 — 사용자가 SQL을 실행했고 REST로 재조회해 `week45_content`에 파트 3 지문 어휘 47행+지문 6행(5단락+출처), `week45_comprehension_questions`에 파트 3 문항 4개 모두 정확히 반영됨을 확인(학습하다 뜻풀이의 "(AI)" 제거도 한/중 둘 다 반영 확인) — **요약하기(4~5주차) 파트 1·2·3 전체 완성. 어휘·지문·이해도 문항 전부 DB 반영·확인 완료**
79. **[출석부 정정 반영 + 랜딩 페이지 디자인 보강]** (1) 정정기간 이후 확정 출석부(출석부_C3.xlsx/C4.xlsx) 전달받아 반영 — 로컬에 python/node가 없어 PowerShell로 xlsx 내부 XML(sharedStrings+sheet1)을 직접 파싱해 데이터 추출(Excel COM 자동화는 Protected View로 추정되는 이유로 실패해 우회). 학번 기준 전수 대조 결과 C3(30명)은 변경 없음, C4는 13명→30명으로 17명 추가(기존 13명은 학번·코드 그대로 유지) — `supabase_students_setup.sql`에 `on conflict (student_id) do nothing` 구조의 추가 insert 블록으로 반영(재실행해도 무해). 이 파일은 학생 실명·학번이 있어 원래부터 gitignore 처리됨. 사용자가 "재수강 학생 따로 표시되지 않지?"라고 확인 요청 — 원본 출석부의 "비고"(재수강 표시) 칸은 애초에 안 가져왔고 `students` 테이블 자체에 그런 컬럼이 없어 시스템 어디에도 재수강 여부가 노출되지 않음을 확인시켜 줌. (2) 사용자가 "메인 화면이 눈에 확 안 들어온다"고 솔직한 의견을 요청 — 화려한 색을 더하자는 게 아니라 카드 구분이 안 되고 도입부가 얇아서라고 진단, 3가지 제안(카드별 단색 아이콘/도입부 보강/순서를 보여주는 연결 화살표)을 모두 반영. `index.html`에 자판·연필·문서줄임 SVG 아이콘(teal 톤, 42px 원형 배지)을 카드마다 추가, 카드 사이에 아래 방향 화살표 연결선 추가, lede 문장을 목적 설명이 담기도록 확장. 이어서 사용자가 "무엇을·왜·어떻게 설명도 넣으면 좋지 않을까"라고 추가 요청 — 카드 목록 위에 `.intro-panel`(무엇을/왜/어떻게 3항목 박스)을 처음 추가했으나, 실제 화면을 본 사용자가 "디자인적으로 별로"라고 지적(라벨과 문단이 옆으로 붙어 있고 "왜"만 짧아 불균형) — 라벨을 알약 배지로 바꾸고 위로 올려 항목 사이 구분선을 넣는 1차 수정. 다시 확인한 사용자가 이번엔 레이아웃이 아니라 **"무엇을/왜/어떻게로 나누는 구성 자체가 촌스럽다"**고 더 근본적인 지적 — 발표 자료 템플릿처럼 읽힌다는 데 동의해 박스 구조를 완전히 없애고, 원래 있던 한 줄짜리 lede 문단에 내용을 자연스러운 흐름으로 녹여 2개 문단(도구 설명 + 사용법 안내, 크기로 위계 구분)으로 재구성. 브라우저 확대(zoom)로 세 버전 모두 렌더링 확인하며 반복 검토
80. **[프로그램 이름 통일 — "성찰적 글쓰기" / "요약을 활용한 글쓰기"]** 사용자가 랜딩 페이지 카드 제목을 "성찰적 글/요약하기"로 둘지 "성찰적 글쓰기/요약하는 글쓰기"로 바꿀지 문의 — 어시스턴트는 교재 원문 표현(3단원 "성찰적 글 계획하기" 등은 "성찰적 글"을, 6단원은 "요약을 활용한 글쓰기"를 씀)과 프로그램 안쪽 제목과의 일관성("성찰적 글쓰기"로만 바꾸면 카드와 안쪽 H1이 달라짐)을 근거로 기존 유지를 추천했으나, 사용자가 "성찰적 글쓰기 / 요약을 활용한 글쓰기"로 최종 확정("요약하는 글쓰기"는 어색해서 교재의 실제 6단원 제목을 그대로 씀). `index.html` 카드 제목, `week2_3_reflective_writing.html`·`week4_5_summary.html`의 `<title>`·`<h1>`·lede·완료 배지까지 세 파일 전부 일관되게 반영 — 단 `PART_DEFS`의 파트별 하위 이름("3. 성찰적 글 계획하기" 등)은 교재 소제목을 그대로 쓰는 §44 원칙에 따라 그대로 둠. 사용자가 파트 선택 화면 스크린샷으로 "여기 앞에 7 빠졌어"(요약을 활용한 글 작성하기1/2에 "7." 단원 번호 누락)를 짚어줘서 파트 2·3 `PART_DEFS` 이름 앞에 "7. "을 추가해 파트 1("6. ...")과 형식 통일. `get_page_text`로 세 파일 전부 재확인해 통일됨을 검증
81. **[랜딩 페이지 설명 문단 결국 원복]** §79~80에서 무엇을/왜/어떻게 박스 → 자연스러운 2문단으로 여러 차례 다듬었으나, 사용자가 최종적으로 "이거 굳이 설명 안 할래, 너무 지저분해 보여"라고 판단 — 두 문단을 다 없애고 원래 있던 한 줄짜리 lede("외국인 유학생 대상 한국어 글쓰기 수업을 위한 사전학습 도구입니다. 아래에서 해당 주차 단계를 선택하세요.")로 되돌림(`.lede-sub` CSS도 정리). "컴퓨터로/수업 전에/성적 무관"같은 안내는 각 프로그램 자체의 "이렇게 진행됩니다" 박스에 이미 있어서(§75) 정보 손실은 없음 — 아이콘·연결 화살표·카드 이름("성찰적 글쓰기"/"요약을 활용한 글쓰기")은 그대로 유지. 이어서 사용자가 카드 아이콘 자체도 "굳이 필요 없을 것 같아, 사실 다 타이핑해야 하잖아"라고 지적 — 세 단계 모두 결국 타이핑이라는 공통 활동인데 아이콘이 서로 다른 종류의 활동처럼 보이게 한다는 정확한 지적이라 아이콘·`.icon-badge`/`.card-top` CSS를 전부 제거하고 배지만 남김(연결 화살표는 유지 — 순서를 보여주는 정보라 다른 성격). 마지막으로 카드 설명 문구에서도 "성찰, 감정 표현·비유"/"요약, 의견 및 근거 제시" 같은 주제 키워드와 "필수 어휘 제공" 상투구를 빼고 "교재 pXX~XX — 파트 구성"만 남기도록 요청 — 반영 완료(1주차는 교재 쪽수가 없는 단원이라 기존 설명 그대로 둠). 사용자가 파트 구성 부분("파트 1(계획하기)·파트 2(작성하기)·파트 3(수정하기)")까지 이어서 필요한지 재질문 — 클릭해서 들어가면 파트 선택 화면에서 동일한 구성이 다시 카드로 나와 중복이라는 데 동의해 랜딩 페이지 설명은 "교재 pXX~XX" 쪽수만 남기고 파트 구성은 제거. lede 문장이 좁은 화면에서 짧은 줄 하나가 뚝 떨어지는 것을 사용자가 지적 — 처음엔 `text-wrap:balance`(h1에도 함께 적용)로 자동 균형을 맞췄으나, 사용자가 "한 문장씩 하는 게 낫지 않아?"라고 재제안해 두 문장 사이에 `<br/>`을 넣어 항상 문장 단위로 줄바꿈되게 확정(balance는 각 문장 자체가 좁은 화면에서 또 줄바꿈될 때를 위해 유지). 1주차 카드 설명 "한글 타이핑이 처음인 학습자를 위한"이라는 문구도 사용자가 "처음 아닌 사람도 있을 수 있잖아"라고 지적 — 실제로는 숙련도와 무관하게 모두가 거치는 단계라 "누구를 위한"이라는 조건을 빼고 "한글 키보드 자판 위치·손가락 사용법 연습"으로 내용만 서술하도록 수정
82. **[연구 참여 동의서 — 텍스트 확정 + 실제 구현]** 배포 완료(§78) 이후 남은 최우선 과제. (1) *KUIRB 확인*: irb.korea.ac.kr 공개 페이지를 직접 확인 — 승인 전 동의 취득은 자료로 쓸 수 없고 심의는 월 1회 주기(제276차 등)라 개강 전 정식 승인은 시간상 불가능함을 확인, "소논문 경험 있는 연구자 2명"의 기존 조언(동의 취득+연구윤리 준수)대로 비정식 경로로 진행하기로 최종 확정(§2에 이미 메모된 방향의 재확인). (2) *형식*: 종이 대신 디지털(프로그램 안에 내장) 동의로 결정 — 로그인 흐름에 자연스럽게 통합되고 유학생에게 서면보다 편함. 참여자 코드당 딱 한 번만 노출(3개 프로그램 중 어디서 먼저 로그인하든 공유), 동의 여부와 무관하게 프로그램 기능·참여 점수는 항상 동일 — 비동의 시엔 그 참여자 데이터만 논문 분석에서 제외. (3) *문구*: 여러 차례 외부 피드백(보관·폐기 기간, 철회의 구체적 방법, 학기말 설문과의 독립성, 성적 무관 강조, 다국어·쉬운 문장, "익명 처리"는 부정확하니 "가명 처리"로 정정 — `students` 테이블이 참여자 코드↔실명을 계속 연결하고 있어 진짜 익명이 아님) 반영해 1~7번 섹션 구조(목적/참여 방법 및 기간/자발적 참여와 수업 평가/이득과 불편/개인정보 보호/철회/문의)로 확정. 이 과정에서 사용자가 제시한 최신 초안의 "1. 연구 목적"에 **한글 타이핑 속도와 정확도**, **수업 중 디지털 글쓰기 활동의 수행**(착수 시간·작성량·결과물 수집), **프로그램 사용 전후 타이핑 검사**가 포함돼 있었는데, 이는 §2에서 이미 확정한 RQ 범위(RQ1 내용 이해도·RQ2 쓰기 효능감 인식)를 벗어나고 실제로 구현된 어떤 프로그램에도 없는 활동(실제 과제물 수집·사전사후 타이핑 검사)이라 불일치를 지적 — 사용자가 "RQ랑 맞춰서 해 줘"라고 확정해 세 항목 모두 제거, 목적을 RQ1·RQ2 두 가지로만 좁히고 참여 방법도 "프로그램 사용 중 자동 기록되는 학습 데이터(퀴즈 응답·진행 상황)"+"학기말 쓰기 인식 설문"으로만 한정. (4) *구현*: 새 테이블 `research_consent`(participant_code PK/agreed boolean/responded_at, `supabase_setup_consent.sql`)를 만들고, 세 프로그램 파일(`stage0_jari_practice.html`/`week2_3_reflective_writing.html`/`week4_5_summary.html`) 모두에 동일한 오버레이(`#consentOverlay`, 한국어·English 탭 전환, 1~7번 전문 + 동의/비동의 버튼)를 추가. 공통 진입점인 `enterProgram(code, name)`(로그인·이어서진행 재개 시 항상 거치는 함수) 안에서 `checkConsentAnswered(code)`로 이미 응답했는지 확인 후, 처음이면 `showConsentOverlay`로 가로챘다가 응답 즉시 `saveConsent`로 저장하고 원래 화면(파트 선택/연습 화면)으로 그대로 진행 — 동의·비동의 어느 쪽을 눌러도 프로그램 진입은 막지 않음. stage0는 `enterProgram` 꼬리 부분(`showFinalSummary`/`showScreen('practice')` 분기)을 `proceedIntoProgram()` 클로저로 감싸 동의 후에도 동일하게 재사용. 브라우저 프리뷰로 세 파일 모두 콘솔 에러 없음과 오버레이 렌더링(한/영 탭 전환 포함)을 스크린샷으로 확인. **아직 안 한 것**: 빈칸으로 채운 참여 기간("2026년 9월~10월, 1~5주차")·설문 시기("2026년 12월경")가 실제 학사일정과 맞는지 최종 확인, Supabase 대시보드에서 `supabase_setup_consent.sql` 실행, git 커밋·배포
83. **[3번·4번 섹션 재검토 — 배점 숫자 제거 + 모순처럼 읽히는 문장 정리]** 사용자가 4번(이득과 불편)을 통째로 빼는 게 낫지 않냐고 문의 — 어시스턴트는 예전 외부 피드백 표에서 "위험/보상 고지 누락"을 명시적으로 지적받아 넣은 항목이라 완전 삭제보다는 "심리적 부담" 문장만 빼고 담백하게 줄이자고 역제안, 사용자가 그 제안을 채택. 3번(자발적 참여와 수업 평가)에서는 사용자가 "참여 점수(10점)"이라고 박아놓은 숫자가 실제로는 7점이라 부정확하고, "성적과 무관"이라면서 "참여 점수엔 반영된다"고 쓴 게 모순 아니냐고 재확인 — 실제로는 모순이 아니라 "동의서 응답 여부"(연구 참여, 자발적)와 "프로그램 사용 여부"(수업 요건, 참여 점수에 반영)가 서로 다른 질문인데 "참여"라는 단어가 양쪽에 다 쓰여서 혼동을 준 것이라고 설명 — 배점 숫자를 아예 빼고(향후 배점이 바뀌어도 동의서를 다시 손댈 필요 없게), 문단 서두에서 "프로그램 사용=수업 요건, 참여 점수에 반영/동의서 질문=그와 별개로 연구 자료 사용 동의 여부"를 먼저 분리해서 명시하도록 재작성. 세 파일(`stage0_jari_practice.html`/`week2_3_reflective_writing.html`/`week4_5_summary.html`) 한국어·English 버전 전부 동일하게 반영, 콘솔 에러 없음 확인
84. **[2번(참여 방법 및 기간) 세부 날짜 삭제 — 학기 전체 기간으로 통합]** 설문지를 정확히 언제 배포할지 아직 확정 못했다는 이유로, "① 9~10월(1~5주차)/② 12월경" 식으로 활동별 세부 시기를 못 박던 것을 사용자가 지적 — 학기 전체 기간("2026년 9월~12월")으로 한 번만 명시하고, 그 안에서 ①프로그램 사용 중 자동 기록·②학기 중 설문(구체적 시점 미명시) 두 방식으로 이루어진다고만 서술하도록 단순화. 나중에 설문 실시 시점이 구체적으로 정해져도 동의서를 다시 손댈 필요 없음. 세 파일 한국어·English 전부 반영, 콘솔 에러 없음 확인
85. **[RQ2·RQ3 측정 도구 설계 — 문항 확정]** §2에서 미착수로 남아있던 부분을 착수. (1) *설문 시행 시점*: 처음엔 "학기 말(12월)"로 막연히 잡았으나, 사용자가 "타이핑 활동에 대한 기억이 흐려질 것"이라고 지적 — 개입 직후(4~5주차/요약을 활용한 글쓰기 전체 완료 시점, 대략 10월 초) 측정하는 것으로 변경, 이게 회상 편향을 줄이는 더 표준적인 방법이라고 확인해줌. 다만 동의서(§82~84)의 참여 기간 문구는 "학기 전체(9~12월)"로 이미 넓게 잡혀 있어 다시 손댈 필요 없다고 정리(사용자 판단). 배포 방식은 동의서와 동일하게 인앱 오버레이로, week4_5_summary.html 완료 시 1회 노출하는 방향으로 합의(아직 미구현). (2) *RQ3 면담*: 사용자가 "면담 꼭 해야 하나"라고 문의 — §2 원안이 애초에 "서술형 문항이나 가벼운 면담" 중 택1이었음을 확인해주고, 개방형 설문 문항만으로 대체(면담 생략) 확정. (3) *문항 초안*: RQ2 리커트 12문항(A 쓰기효능감 4·B 쓰기불안 4·C 프로그램의 인지된 도움 4, 5점 척도)과 RQ3 개방형 4문항을 작성. (4) *다른 AI 교차검증*: 사용자가 제미나이·GPT에도 같은 문항을 보여주고 의견을 구함(연구 배경·RQ·이론적 배경·문항 전문을 담은 복붙용 프롬프트를 어시스턴트가 작성해줌). 제미나이 피드백을 검토한 결과, ① A·B 문항을 절대적 현재 상태("나는 ~할 수 있다")가 아니라 회고적 변화 인식("이 프로그램을 사용한 후 ~하게 되었다/줄었다")으로 바꾸는 게 사전조사 없는 설계의 한계(개입 효과와 개인 성향 분리 불가)를 보완하는 실질적 개선이라 채택(부수적으로 B군 역채점 필요성도 사라짐), ② A-4를 또래비교 문항("다른 학생만큼")에서 과제특정 효능감 문항("수업에서 요구하는 글을 쓸 수 있다")으로 수정(Bandura의 과제특정성 원칙에 부합), ③ B-7의 주체 모호성("이해하기 어려울까봐"가 내가 지문을 이해 못하는 건지 남이 내 글을 이해 못하는 건지 불분명) 수정, ④ RQ3 3번(가정형 "이 프로그램이 없었다면?")을 "눈으로 읽기 vs 타이핑하며 읽기 차이"로 교체(이론적 배경 2번의 "적극적 노출·주의집중" 주장을 학생 언어로 직접 확보 가능) — 전부 채택. 반면 "크론바흐 알파·탐색적 요인분석(EFA)에 이상적인 문항 수"라는 제미나이의 주장은 참여자 규모(최대 60명, 동의자·응답자는 더 적음)로는 EFA에 필요한 표본(문항당 5~10명 이상, 안정적이려면 100명 이상)에 크게 못 미쳐 과장된 조언이라고 판단, 채택하지 않음(논문엔 하위척도별 평균·Cronbach's α 신뢰도, C군과 A/B군의 상관·회귀 정도가 현실적 분석 수준이라고 안내). GPT 의견은 아직 대기 중 — 받으면 이어서 검토 예정
86. **[RQ2·RQ3 설문 문항의 이론적 근거 — 실제 논문 여부 웹 검색으로 검증]** 사용자가 "설문 문항이 어떤 이론에 근거해야 하는지" 질문 — 소논문 Method(연구방법)에는 각 문항군이 어떤 기존 척도/이론에 근거했는지 밝혀야 함을 설명. 이어서 사용자가 "확실히 있는 논문이야? 거짓이면 절대 안 돼"라고 재차 확인 요청 — 기억에 의존해 답하지 않고 WebSearch로 4개 인용 전부 실제 존재 여부·정확한 서지사항(저널명·권·호·페이지)을 직접 검증. 그 결과 A(쓰기 효능감)는 Pajares & Valiante(1997, *Journal of Educational Research*, 90, 353–360)와 Woodrow(2011, *System*, 39(4), 510–522), B(쓰기 불안)는 Cheng(2004)의 SLWAI(*Journal of Second Language Writing*, 13(4), 313–335), C(프로그램의 인지된 도움)는 Davis(1989)의 기술수용모델(TAM) 지각된 유용성 개념(*MIS Quarterly*, 13(3), 319–340)이 모두 실재하는 논문임을 확인(검색 중 Pajares & Valiante의 정확한 논문 제목이 처음 언급과 살짝 달랐던 것도 정정: "Influence of writing self-efficacy beliefs on the writing performance of upper elementary students"). C군은 기존 심리척도가 아니라 연구자가 이 처치(프로그램)에 맞춰 개발한 문항이라 TAM은 "참고 개념"으로만 인용해야 함을 명확히 함
87. **[GPT 교차검토 반영 — 사전-사후 설계로 전환, 문항 최종 확정, RQ1 3단계 측정 제안 기각]** 사용자가 제미나이에 이어 GPT에도 같은 자료를 보여주고 받은 상세 피드백을 검토. (1) *가장 중요한 지적*: 사후 단일 측정(회고적 문항 포함)으로는 "변화"·"긍정적 영향"을 주장할 근거가 안 된다는 지적이 제미나이보다 훨씬 날카로웠음 — 채택하여 **A·B 8문항을 진짜 사전-사후(프로그램 첫 로그인 시 1회 + 4~5주차 전체 완료 시 1회, 절대적 현재상태 문장으로 되돌림)로 전환** 확정. 동의서 인프라(참여자 코드당 1회 오버레이)를 그대로 재사용 가능해 구현 부담은 크지 않다고 판단. (2) A-4 대체 문항은 제미나이안("대학 수업에서 요구하는 글을 쓸 수 있다")보다 GPT안("글쓰기 과제를 받으면 오래 걸리지 않고 시작할 수 있다")을 채택 — §2 이론적 배경의 "글쓰기 착수 지연"과 직접 연결됨. B-7도 GPT안("한글 타이핑이 느려서 내용을 충분히 못 쓸까봐")으로 교체 — 이 연구의 핵심 기제(타자 부담→인지적 에너지 소모)에 더 정확히 닿음. C를 C-1(수업내용 이해 도움 3문항)·C-2(타이핑·글쓰기 지원 도움 3문항)로 분리(기존 12번 문항이 "타자가 편해짐"+"내용에 집중함" 두 주장을 한 문장에 묻는 이중질문이었던 문제 해결). D(만족도) 3문항 신설, RQ3 "추천하시겠어요?" 개방형은 담당 교수=연구자라는 이중 역할 때문에 사회적 바람직성 편향 위험이 있다는 GPT 지적을 채택해 만족도 D군의 리커트 문항으로 이동, RQ3 개방형은 다른 문항(원문-타이핑 비교 등, 이미 §85에서 채택)으로 대체. (3) *EFA 재확인*: GPT도 독립적으로 "60명으론 새 척도 구조 확정 주장은 신중해야" 지적(MacCallum et al. 1999 인용) — 제미나이 검토 때 이미 내린 판단과 일치, 하위척도별 평균·Cronbach's α까지만 보고하는 방침 재확인. (4) *RQ1도 사전-사후로?*: GPT가 "타이핑 전 사전확인 2~3문항 + 직후 확인(기존) + 수업 시작 직전 지연확인 2문항"의 3단계 측정을 제안 — 사용자가 "우리 강의실에서 바로 물어볼 이유가 없는게 어차피 그 지문을 가지고 내가 설명을 하거든"이라고 정확히 반박: 이 프로그램은 애초에 "수업 직전에 하는 사전학습"으로 설계돼 있어 "즉시 확인"과 "수업 직전 확인" 시점이 사실상 같아져 3번째 측정점이 무의미함. 남는 "활동 전 사전확인"도 학생이 아직 안 읽은 지문 내용을 묻는 거라 바닥효과만 확인하는 셈이라 정보가치가 낮다고 판단 — RQ1은 기존 방식(개입 직후 단일 측정) 유지, 논문 Limitations에 이유를 "시간이 없어서"가 아니라 "사전-사후 비교를 만들어도 의미 있는 데이터가 안 나와서"로 명시하기로 확정. (5) *추가 인용 검증*: 사용자가 "확실히 있는 논문이야?" 재차 확인 — GPT가 제시한 나머지 5개 핵심 문헌(Cheng 2004; Teng, Sun, & Xu 2018, *TESOL Quarterly*, 52(4), 911–942; Polit & Beck 2006, *Research in Nursing & Health*, 29(5), 489–497; Park, Sha, & Pan 2014, *International Journal of Social Research Methodology*, 17(6), 643–658; Hill 2020, *International Journal of Behavioral Development*, 44(2), 184–191)도 전부 WebSearch로 실재 확인, 정확한 서지사항 사용자에게 전달. (6) *최종 문항 확정*: 사전 설문(A·B 8문항), 사후 설문(A·B 재측정 8 + C 6 + D 3 + 개방형 4 = 21문항, 5~8분). 사용자가 "이대로라면 논문 쓰는 데 문제없는 거 맞지?"라고 최종 확인 — "문제없다, 다만 (a) N=60이라 요인분석 불가 (b) 통제집단 없음 (c) 연구자=담당교수로 인한 응답편향 가능성 (d) RQ1 사후 단일측정, 이 4가지는 논문 Limitations에 정직하게 써야 한다"고 답해 최종 승인받음
88. **[RQ2 사전-사후 설문 구현 완료]** §87에서 확정된 문항을 실제 코드로 구현. 새 테이블 `research_survey`(participant_code+phase['pre'/'post']+answers jsonb, `unique(participant_code, phase)`로 phase당 1행 보장, `supabase_setup_survey.sql`)를 만들고, 콘텐츠(문항 KO/EN, 리커트 렌더링 함수, 서술형 렌더링 함수, `checkSurveyDone`/`saveSurvey`)를 세 파일 모두에 동일하게(1주차·2~3주차는 사전 설문만, 4~5주차는 사전+사후 둘 다) 추가. 게이팅 지점: (1) 사전 설문 — `enterProgram()` 안에서 동의서 응답 직후(`afterConsent()`) `checkSurveyDone(code,'pre')`로 확인 후 안 했으면 `showPreSurveyOverlay` 노출, 세 파일 공통 패턴. (2) 사후 설문 — 4~5주차 전용, `showPartSelect()`가 렌더링될 때마다(파트/퀴즈를 마치고 돌아올 때마다 호출됨) `maybeShowPostSurvey()`를 실행해 "3개 파트 전부 완료 + 아직 phase='post' 기록 없음"이면 그 위에 사후 설문 오버레이를 띄움 — 파트 완료 감지를 위한 새 이벤트훅을 따로 만들 필요 없이 기존 화면 재렌더링 시점에 자연스럽게 검사하는 방식. 리커트 문항은 라디오 버튼(1~5) + 상단 안내문, 서술형은 textarea — 전부 답하지 않으면 제출 막고 에러 문구 표시. 브라우저에서 임시 디버그 훅(`window.__testShowPreSurvey`/`__testShowPostSurvey`)으로 렌더링(문항 수: 사전 8개, 사후 리커트 17개+서술형 4개 정확히 일치 확인)·미답변 시 제출 차단·정상 제출 시 오버레이 닫힘과 콜백 실행까지 전부 확인 후 디버그 훅 제거. 세 파일 모두 콘솔 에러 없음 확인. **아직 안 한 것**: Supabase에 `supabase_setup_survey.sql` 실행, git 커밋·배포
89. **[사후 설문 트리거 방식 수정 — 앱 자동 노출 → 별도 페이지로 전면 재설계]** §88에서 만든 "4~5주차 전체 완료 시 자동 노출" 방식을 사용자가 즉시 지적: "사후 설문조사는 내가 수업까지 마치고 학생들이 글쓰기 다 해보고 한 다음에 하는 게 맞아... 지금 프로그램에 바로 하게 해 버리면 글쓰기 전 설문조사가 되어버려." 정확한 지적 — 이 프로그램은 애초에 "수업 전에 미리 하는 사전학습"이라, 앱이 "4~5주차 다 끝냄"을 감지하는 시점은 학생이 아직 실제 수업에서 글쓰기를 해보기 *전*이다. 사후 설문이 측정하려는 건 "실제 글쓰기까지 해본 뒤의 효능감·부담감·도움 인식"이라 이 시점에 물으면 RQ2 설계 취지 자체가 무너짐. 앱은 "그 수업이 실제로 언제 열리는지, 글쓰기 활동을 언제 마쳤는지"를 알 방법이 전혀 없음(교실 안에서 일어나는 일이라 인앱 이벤트로 감지 불가) — 그래서 해결책을 "교수자가 직접 배포 시점을 정하는 독립 페이지"로 전환. **조치**: (1) week4_5_summary.html에서 사후 설문 전용 코드(`SURVEY_ITEMS_C`/`D`/`OPEN`, `showPostSurveyOverlay`, `maybeShowPostSurvey`, `showPartSelect()`의 자동 호출)를 전부 제거 — 이 파일은 이제 사전 설문만 가진 나머지 두 파일과 동일한 구조로 되돌아감. (2) 새 파일 `survey_post.html` 신설 — 독립적인 분반/이름/학번 확인 로그인 화면(기존 프로그램들의 로그인 패턴 재사용, 자체 등록 기능은 뺌 — 이미 다른 프로그램에서 등록된 학생만 대상) 다음 곧바로 사후 설문(A·B 재측정 8 + C 6 + D 3 + 개방형 4 = 21문항, 그룹 라벨 표시)이 나오고, 제출하면 "제출 완료" 화면, 이미 제출했으면 "이미 참여하셨습니다" 화면으로 분기. 배포는 랜딩 페이지(index.html)에 링크를 두지 않고, **교수자가 실제 수업(또는 다음 회차 수업)에서 링크·QR코드로 학생들에게 직접 공유**하는 방식으로 확정 — 이렇게 하면 "실제 글쓰기까지 마친 뒤"라는 타이밍을 교수자가 완전히 통제할 수 있음. 브라우저에서 실제 Supabase 학생 명단이 정상 로드됨을 확인, 임시 디버그 훅으로 문항 렌더링(리커트 17+서술형 4 정확히 일치)·미답변 시 제출 차단·저장 실패 시 에러 문구 표시 및 버튼 재활성화(사용자가 재시도할 수 있게)까지 확인 후 디버그 훅 제거, 콘솔 에러 없음 확인. **아직 안 한 것**: Supabase에 `supabase_setup_survey.sql` 실행(§88과 동일 테이블 공유), git 커밋·배포

## 6. 아직 정해지지 않은 것

- 번역(영어·중국어) AI 교차 검토는 완료·반영되었고 실제 DB에도 반영 완료됨(§5 항목 48). 중국어 원어민의 최종 감수만 아직 필요
- 2~3주차(성찰적 글) 파트 1·2·3 어휘 큐레이션·구현, 이해도 확인 퀴즈(RQ1 측정 도구) 구현 모두 완료(§5 항목 49). 여러 파트에 걸쳐 같은 단어가 다시 나올 때 처리 방식도 확정(`week23_vocabulary.md`·`논문자료/어휘_선정_기록.md` §7 참고)
- **IRB(생명윤리위원회) 심의 필요 여부** — 소논문 투고 학술지가 아직 미확정이라 사용자가 직접 확인 중. 확인 결과에 따라 동의서(연구윤리 문서) 형식·시점이 달라짐
- **RQ2 측정 도구(학기 말 리커트 척도 설문)** 문항 설계 아직 미착수
- **RQ3 측정 도구(학기 말 개방형 문항/인터뷰)** 설계 아직 미착수 — 위 IRB 확인 이후 동의서와 함께 진행 예정
- `research_export.html`에 `quiz_results` 내려받기 버튼 추가 필요 (다른 테이블은 이미 있음)
- 잠긴 파트(아직 진행 안 한 학생 기준)를 검토·미리보기할 방법 — 진행 기록 저장 없이 내용만 보는 "미리보기" 기능을 제안했으나 사용자가 아직 결정 안 함
- **4~5주차(요약하기) 제작 — 파트 1·2·3 전부 완료**. 3파트 구조(파트1 45~54쪽/파트2 55~61쪽/파트3 62~64쪽)로 확정(§5 항목 76). 파트 1·2는 어휘·지문·이해도 문항까지 SQL·앱·DB 전부 반영·확인 완료(§5 항목 57~61, 76~77). 파트 3(AI 지문 어휘 47개+지문+이해도 문항 4개)도 SQL·앱 구현 완료, 64쪽 문어체 코너는 프로그램에서 제외하기로 확정(§5 항목 78) — **사용자가 마지막으로 SQL만 실행하면 요약하기 프로그램 전체가 완성됨**. 남은 건 git 커밋·푸시·배포뿐(§6 별도 항목)
- localStorage 로그인 저장은 사용자가 실제 사용 중 정상 작동을 직접 확인함(§5 항목 64) — 이어서 폼이 같이 보이는 문제를 발견·수정까지 완료. `week4_5_summary.html`의 로드맵 퀴즈 칩은 아직 실제 사용 화면으로 확인된 적은 없음(코드 정적 검토로만 정합성 확인, §5 항목 61) — 아직 큰 문제 보고 없음
- **[중요] 2~3주차·4~5주차를 "밀도 있고 탄탄하게" 구성하는 것이 이제 핵심 우선순위** — 4번째 단위(보고서 쓰기)를 만드는 대신, 사용자가 "연구 주제와 연구 문제에 맞춰 2·3(성찰적 글·요약하기)을 탄탄하게 구성해야겠다"고 방향을 명확히 함(2026-08-27). 1차 실행으로 RQ1 측정의 구성 타당도 문제를 짚어 지문 읽기 화면을 추가함(§5 항목 55). 남은 후보: 어휘 큐레이션 엄밀성 재점검, 참여자 데이터 완결성 모니터링, 4~5주차 설계를 처음부터 탄탄하게(완전성 검증+이해도 퀴즈+읽기 화면까지 한 번에)
- **RQ1 "내용 이해" 문항** — 구현·검증 완료(§5 항목 56, 성찰적 글 파트 2용 4문항). 4~5주차(요약하기)에도 파트 1용 4문항 반영 완료(`supabase_setup_week45.sql`)
- **RQ1의 인과 주장 근거 부족** — 지금 설계는 사전/대조군 없는 사후 단일 측정이라 "타이핑이 이해도를 높였다"는 인과적 주장을 뒷받침하기엔 약함. 서술적 결과로 프레이밍하거나 비교 기준을 추가하는 방안을 검토해야 함 — 아직 미착수
- 교재 원문 반영 시점과 방식 (필요할 때마다 해당 쪽수만 전달받기로 함)
- 시험 주간·온라인 강의 주간(주5 월요일, 주8 전체, 주12~13 경계)의 사전학습 처리 방식
- 참여자 기록 조회 범위(현재: 참여자 코드만 알면 상호 조회 가능한 상태로 유지하기로 함 — 소규모 통제된 연구 환경이라는 전제)
- 정정기간 이후 확정 출석부 반영 (사용자가 확정되면 전달 예정 — 학번 기준 upsert로 기존 코드·기록 유지하며 반영)

## 7. 참고 링크

- GitHub: https://github.com/214soundi-ship-it/hangul-typing-practice
- 배포: https://hangul-typing-practice.vercel.app
- Supabase 프로젝트: https://lkimtjqooxzvlbnigqyk.supabase.co
